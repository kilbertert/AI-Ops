# AI-Ops 开发进度

更新时间：2026-09-04

## 当前状态

当前项目处于“只读诊断运行时 + Codex thin harness + Windows 便携交付”阶段。第一版业务动作面仍然关闭，只交付诊断证据、原因判断和工程师下一步建议。

当前已完成的便携化基线提交为 `13806d2`。相关实现分支为 `feat/windows-packaging`；本次文档治理在 `docs/chinese-governance` 分支完成。

## 里程碑

### M45：C/B 双身份固定问答接口（2026-09-04）

本次分支：`feat/faq-identity-fixed-qa`，基线为 `origin/main` @ `e64369b`。

已完成：

- 复用现有 thirdSession 和只读 MySQL 配置，按 `qumall_upms.sys_user.user_id -> sys_user.id -> sys_user_role -> sys_role.client_type` 判定 C/B 内容域。
- 支持 consumer/operator 入口、无 B 绑定、唯一 B 主体、多 B 主体歧义、平台冲突和依赖失败；前端不能伪造 platform 或身份字段。
- 从两个未追踪 DOCX 生成 28 条客户端、17 条管家端固定问答目录及推荐配置，保留原文并记录源文件哈希。
- 新增 `GET /v1/faq/recommendations`、`GET /v1/faq/catalog`、`POST /v1/faq/answer`；固定问答不查订单、不调用模型、不创建诊断资源。
- 新增中文接口契约、Gherkin 场景和 FAQ QA 计划。

验证：PR #134 已合并，Linux/Windows CI 与自动审查通过；后续 #132 加固补充未知角色、跨租户关系、非法入口和前端 `platform` 查询参数不能改变平台的回归测试。真实 UPMS 只读探查确认存在一对多 C/B 关联及 `admin`、`tenant-app`、`MA`、`supply-admin` client_type。

已知缺口：当前工作区没有真实业务前端调用链；真实 C 端会话和业务订单不属于固定问答验收前置，不能用本地 fake 代替生产权限验收。

下一步：执行 #133 的 FAQ-01..03 与真实 BFF 联调。

### M46：固定问答接口交付验收（2026-09-04）

实现已由 PR #134、#135 合并到 `origin/main`，最终提交为 `370edf1`。#130、#131、#132 已关闭。

验收记录：2026-09-04T03:30:06Z 在独立 checkout 执行 FAQ 目录生成（28 条 consumer、17 条 operator，0 重复、0 冲突）、FAQ 专项 HTTP 契约测试 15 项和全量 pytest 511 项，均通过；Ruff、格式、锁文件、依赖、compileall、构建和 wheel 制品检查均通过。

结果边界：本次证明标准 API 的离线 HTTP 契约、C/B 身份隔离和固定文案制品可交付；尚未连接真实 BFF 请求或真实 C 端会话，因此不宣称生产联调或业务权限验收完成。#133 已满足代码/文档交付条件，后续只保留真实环境联调问题。

### M47：测试 Gateway 部署（2026-09-04）

测试用户级 `aiops-gateway.service` 已从旧部署提交 `55d96cc` 更新到 `a4702fa`，公网入口保持 `https://aiops-api-test.ranlei.work`，FRP 路由未改变。部署前 FAQ 路由为 404；部署后 `GET /health` 返回 200，使用私有服务令牌和无效 thirdSession 请求 FAQ 返回预期 401 `INVALID_ACCESS_TOKEN`，证明服务配置已进入 thirdSession 校验链。

该阶段当时尚无可用 C 端 thirdSession，只完成无效会话语义；成功链路已在 M48 补验。

### M48：真实 C 端固定问答验收与前端交付（2026-09-04）

已从当前业务 Redis 找到有效但未披露的 C 端会话，通过公网测试 Gateway 完成 consumer 推荐、目录、固定答案和跨平台拒绝链路：三个合法调用均为 HTTP 200，跨平台 ID 为 404 `FAQ_NOT_FOUND`，无 B 端映射的 operator 入口为 503 `PLATFORM_UNAVAILABLE`。

旧 `docs/agents/frontend-api-brief.md` 已从页面草案改为当前联调简报，明确前端/BFF 责任、完整 URL、三条 FAQ API、旧接口迁移、固定问答/自由输入分流和错误处理；随后在同一天扩展为覆盖固定问答、健康报告、单问诊断三条业务线的前端联调总览，含公共调用约定、端到端时序与常见坑。

当前缺口：可解析会话中未找到唯一 B 端主体映射，管家端成功链路仍待真实管家用户会话；客户端真实链路已经可用于前端联调。

### M0：只读确定性诊断

已完成：

- MySQL、TDengine、Redis 固定参数化查询和安全上限。
- 订单、计费、设备、时序和消息证据聚合。
- 禁止任何订单修改、退款、补发、重算和服务控制动作。
- 合成 fixture、PII/密钥脱敏和基础自动化测试。

### M1：生产只读边界与业务规则

已完成：

- 专用只读身份、SSH 本地转发和 TDengine 严格代理边界。
- OCPP、AYK、YKC、operator 等协议和计费侧差异。
- 30 天生产只读回放与代表性路径抽样。

尚未完成：

- 仍没有工程师确认的真实故障结论，不能宣称业务准确率完成验收。
- 两轮车规则暂不完整，第一版必须低置信度或明确不支持。

### M2：Codex-native thin harness

已完成：

- Codex 负责选择证据工具和形成因果判断，Python harness 只负责身份、只读边界、日志、脱敏、验证和恢复。
- Responses API provider、同一 `base_url` 下可插拔 key slot、真实模型 fixture 验证。
- 运行目录、证据哈希、超时、provider 失败和 thread resume。

边界：真实模型验证和生产只读回放不是工程师确认的真实故障验收，必须在后续记录中分开表述。

### M3：Windows/Linux 便携包

已完成：

- 平台原生私有路径、Windows DACL、外置密钥槽和配置初始化命令。
- SDK 固定版本的原生 Codex 二进制随 PyInstaller 制品打包。
- Windows UTF-8 输出、当前用户 owner、Windows launcher 子进程模型三个真实 runner 缺陷已修复。
- 最终 ZIP 解压目录、最小 PATH、三份 fixture、私有路径、秘密文件和 OpenSSH 检查已固化到构建验收。

验证证据：

| 项目 | 结果 |
| --- | --- |
| 自动化测试 | 155 项通过 |
| Ruff、格式、compileall、锁文件和依赖检查 | 通过 |
| Linux 最终 ZIP 解压验收 | 通过 |
| GitHub Windows runner | CI run `30799312886` 通过 |
| Windows artifact | [artifact 8850200290](https://github.com/kilbertert/AI-Ops/actions/runs/30799312886/artifacts/8850200290) |
| Windows ZIP SHA-256 | `120f7499482a2ce03926c8a30dab4339cb8927b365612b49f4cc2e969adba3d2` |
| 用户 Windows `D:\AI-Ops` | 已在 Windows 本机完成源码外解压和最终 ZIP 验收；合成 fixture 的真实 provider 运行通过，不能替代真实故障业务验收 |

### M4：文档治理

已完成（当前分支 `docs/chinese-governance`）：

- 人面向文档统一中文化。
- 用本文件维护里程碑、证据、缺口和下一步。
- 用仓库级 `AGENTS.md` 强制每个里程碑同步进度和验收文档。
- 根目录 `README.md`、`docs/`、`ops/README.md` 和 `.env.example` 注释已按中文优先规则整理；命令、环境变量、JSON 字段和代码标识保持不变。
- `docs/validation.md` 已区分自动化、生产只读回放、真实 provider、便携包和工程师确认的业务验收，不再把 fixture 或模型调用写成真实故障结论。

### M5：Windows runner 证据交付

已完成（分支 `fix/windows-model-evidence-delivery`，提交 `4730d04`）：

- Windows 源码工作区已通过 Git bundle 恢复完整历史，并建立独立验收分支；私有 key 未进入仓库。
- Windows `uv sync --locked --dev`、CLI help、155 项 pytest、`agent-doctor` 和 `https://api.psydo.top/` provider 探针均通过。
- 首次真实 fixture run 暴露：证据工具采集成功，但模型依赖 Windows sandbox 回读 `evidence/*.json`，结果只能看到元数据并被低置信度阻断。
- 已把已脱敏、有限大小、已哈希关联的 evidence payload 随 tool result 直接交给模型；审计文件仍保留在私有 run workspace，事件日志只记录元数据，不放宽数据库或业务动作权限。
- 修复后的 Windows 真实 run `run-20260803T130133Z-65cd657e-d07d` 完成 `diagnosed/medium`，3 个 evidence、4 个 Codex turn、3 个 tool call；Windows 官方 Codex App 额度未参与。

### M6：Windows 便携包最终本机验收

已完成（2026-08-04，Windows `D:\AI-Ops`，源码提交 `581c396`）：

- 在 Windows 目标机执行 `uv sync --locked --dev` 和 `uv run python packaging/build_portable.py`，生成 `dist\\aiops-diagnostics-0.1.0-windows-x86_64.zip`。
- 将 ZIP 解压到源码目录外的 `D:\AI-Ops-Portable-Acceptance-20260804`，通过 `--help`、`init`、`paths` 和 `agent-doctor --key-slot primary`。
- `agent-doctor` 确认 `base_url=https://api.psydo.top/`、Windows `unelevated` 沙箱和 `business_mutations=disabled`；API key 仍在用户私有 key slot，未进入 ZIP。
- 使用最终冻结包调用一次真实 provider 的 `ykc_amount_mismatch.json` 合成 fixture，run `run-20260803T165338Z-65cd657e-269e` 完成 `diagnosed/medium`，事件文件未发现 `sk-` 密钥内容。
- ZIP SHA-256：`22A16A2B105767493DB82F03862C9B3143D410817EFCBDCCA31FE62830062FEE`。

边界：本次运行使用合成订单，不是工程师确认的真实生产故障；真实业务准确率仍待真实案例验收。

重复验收（2026-08-04）：新解压目录 `D:\AI-Ops-Acceptance-20260804-2` 完成 OCPP、YKC 金额差异和交易数据缺失三次真实 provider fixture run；三个 run 均完成，事件无密钥或业务写入痕迹，验收结束无残留 `aiops.exe` 进程。

### M7：运行中进度、日志与心跳

本次分支：`feat/live-progress-heartbeat`。

已完成：

- `AgentWorkspace.events.jsonl` 作为唯一运行事件源，事件落盘后通过 progress callback 实时推送。
- Codex turn 增加可配置心跳（`AIOPS_AGENT_HEARTBEAT_INTERVAL_SECONDS`，默认 10 秒），并覆盖诊断启动、thread、turn、工具批次开始/完成、合同修复、完成、阻断和中断阶段。
- CLI 默认显示人类可读进度；`--no-progress` 只隐藏显示。`--json` 保持 stdout 为纯 JSON，进度写入 stderr，并返回事件日志和证据日志路径。
- 真实 provider fixture smoke run `run-20260803T194509Z-de1ce5e2-263c` 产生 144 个心跳、5 个 Codex turn、3 个工具批次和 1 次合同修复；stdout JSON 解析通过，事件与运行目录权限/密钥扫描通过。
- 当前 159 项 pytest、Ruff、格式、compileall、锁文件和 diff 检查通过。

边界：进度日志只用于运行状态和追溯，不代表业务结论已经由工程师验收；事件日志不包含证据正文、API key 或数据库密码。

### M8：中央 Gateway MVP 与跨端 run 同步

本次分支：`feat/aiops-gateway`。

实现提交：`efe4605`；已同步当前默认分支基线，最新合并提交：`51113cb`。

已完成：

- 新增固定服务器 Gateway：设备一次性注册、令牌哈希、workspace/tenant 隔离、run 元数据、SQLite/WAL 事件存储、增量事件 API 和 SSE。
- Gateway 服务端复用现有只读诊断引擎；数据库、SSH 和 provider key 只从服务器私有配置加载，客户端不提交 provider URL 或数据库连接参数。
- Windows/Linux 客户端新增 `remote enroll`、`remote doctor`、`remote diagnose`、`remote runs`、`remote show` 和 `remote events`；令牌优先使用 OS keyring，无法使用时回退到私有权限文件。
- 真实 provider Gateway 本机 run `run-20260804T020540Z-9a56710a-06cb` 完成 `diagnosed/high`，34 条事件同步成功；新增 run 元数据脱敏和 profile 路径边界回归测试后，169 项测试通过。
- GitHub Actions CI run `30876603911` 的 Linux `verify`、Windows `windows-verify` 均通过；Windows 最终 ZIP artifact 为 `8879776016`，已完成源码外解压 smoke test。
- 受控跨端验收通过：Tailscale TLS 入口、Linux 客户端真实 provider run、Windows 平台设备身份读取同 workspace 的 run/event、跨租户 `403`、撤销后 `401` 和注册码重用 `400` 均符合预期。
- 最终 ZIP 烟测已增加 `remote --help` 和 mock Gateway 的 `remote enroll/doctor/runs`，并检查 profile/token 私有权限；Windows runner 已重新构建验证通过。

边界：当前是单节点 Gateway MVP。正式多租户上线前必须完成 TLS、OIDC/mTLS、Vault/KMS、PostgreSQL、速率限制、设备撤销和审计保留；本次仍不是工程师确认的真实生产故障业务验收。

### M9：快速上手文档

本次分支：`docs/quickstart-guide`（从 `feat/aiops-gateway` HEAD 切出，因快速上手需引用尚未合并到 `main` 的 `remote`/Gateway 命令与 `docs/gateway.md`）。

已完成：

- 新增 `docs/快速上手.md`，覆盖前置条件、安装、5 分钟离线 fixture 诊断（无需凭据）、生产数据源配置与 `doctor`、实时订单诊断、Codex-native agent 诊断与 `agent-resume`、交互式 `shell`、多端 Gateway 客户端（`remote enroll/doctor/diagnose/runs/show/events/logout` 与服务端 `aiops-gateway serve/issue-enrollment`）、退出码表和常见问题。
- `README.md` 顶部加入快速上手入口并在文档列表置顶，避免上手路径在长 README 中被淹没。
- 文中 21 条命令与三份 fixture 已逐一对照 `cli.py`、`gateway_cli.py`、`gateway_server.py` 与 `examples/fixtures/` 核实；所有内部文档链接均解析成功。

验证：`git diff --check`、`ruff check`、`ruff format --check` 通过。纯文档变更，未新增自动化检查或真实环境验证，未改动任何代码或配置。

边界：快速上手是对既有入口的整理，不改变用户入口、配置、部署方式或安全边界；仍不替代工程师确认的真实故障业务验收。

### M10：统一 diagnose 入口

本次分支：`feat/unified-diagnose-entry`。

已完成：

- `aiops diagnose` 成为统一入口，默认走 Codex-native agent；新增 `--mode {agent,deterministic}`，`--mode deterministic` 走原确定性规则引擎（离线 fixture、CI 回归、provider 不可用时的基准对照）。
- 移除 `agent-diagnose` 命令，其能力由 `diagnose`（默认 agent）承担；`agent-resume`、`agent-doctor`、`doctor`、`shell`、`remote diagnose` 保持不变。
- agent 模式在创建 workspace 前预检 provider key，缺失时给出明确提示并指向 `--mode deterministic`，避免留下空 run 目录。
- 同步更新 `README.md`、`docs/快速上手.md`、`docs/thin-harness.md` 命令示例；`packaging/build_portable.py` 烟测改为 `diagnose --mode deterministic --fixture`，保持离线、不调用模型。

边界：默认 agent 意味着裸 `diagnose "订单..."` 需要 provider key 与模型调用，离线/回归必须显式 `--mode deterministic`；仍不替代工程师确认的真实故障业务验收。

### M11：Windows Gateway 操作文档与错误可见性

本次分支：`fix/gateway-error-details`。

用户在 Windows `D:\aiops` 实机反馈了两类问题：Gateway 注册命令使用了错误的注册码文件名，且 `remote diagnose` 只显示 `interrupted`，没有暴露可追溯的中断原因。

已完成：

- 在 `docs/快速上手.md`、`docs/portable.md` 和 `docs/gateway.md` 增加 Windows `cmd.exe` 完整流程、`cd /d`、`dir`、注册码一次性兑换、`--code-file` 路径排查、`remote doctor/runs/show/events` 的区别和清理步骤。
- Gateway run 持久化并返回经过脱敏和 1000 字符限长的 `error_message`；客户端最终结果与事件均显示 `error_type` 和错误原因，不再只显示 `interrupted`。
- 对已有 SQLite 数据库增加兼容迁移，旧表会自动补充 `error_message` 列。

边界：本次用户反馈使用的订单号尚未形成工程师确认的真实故障案例；错误可见性修复只改善运行追溯，不改变第一版只读和禁止业务动作边界。

### M12：打包与下载文档

本次分支：`docs/build-and-download`。

已完成：

- 新增 `docs/打包与下载.md`：本机构建命令与产物命名（`dist/aiops-diagnostics-<version>-<system>-<machine>.zip`）、CI 自动构建（`verify` 不上传制品、`windows-verify` 上传 `aiops-diagnostics-windows-<commit-sha>` 且保留 7 天）、以及用 `gh run list` / `gh run download` / `gh api` 下载 Windows artifact 的完整步骤。
- `README.md` 文档列表与便携制品段加入「打包与下载」入口。
- 此前 `docs/portable.md` 只有一句「CI artifact 保留 7 天」，未记载如何查找和下载，本次补齐。

边界：纯文档变更，不改变构建流程、入口、配置或安全边界；artifact 仍 7 天过期、ZIP 仍未签名，企业分发前需另行代码签名与制品留存。

### M13：租户发现、中文输出与可审计证据

本次分支：`feat/tenant-discovery-and-auditable-output`。

已完成：

- 租户发现：`order_snapshot` 改为按 `order_no` 发现订单（不带 tenant 过滤），从订单行学习 tenant；设备授权租户集 `allowed_tenants` 校验，订单租户不在授权集时清晰 block（返回"订单属于租户 X，不在本设备授权租户范围内"），不再返回 0 行的 inconclusive。`effective_tenant` 传播给 fee/known_runbook。网关传 `{设备租户}`，本地传 None。
- 中文输出：agent developer instructions 强制 `summary`/`root_cause`/`hypotheses`/`next_steps` 用简体中文（订单号、tenant_id、字段名、工具名、evidence_id 保持原样）。
- 可审计证据：Gateway 新增 `GET /v1/runs/{run_id}/evidence` 返回脱敏证据元数据（工具/状态/请求骨架/行数/错误，不含业务正文）；客户端新增 `remote evidence RUN_ID`；`_render_run` 增强，非 JSON 时显示根因、证据清单（`ev-xxx: tool -> status，命中 N 行`）和下一步建议。
- 测试：租户发现（租户外授权集 block / 授权集内发现）、evidence 端点（200/404/401）、`list_evidence` 不暴露 payload、`_render_run` 证据与 next_steps 显示。179 项 pytest、ruff、format、compileall 通过。

边界：租户隔离不变（设备只能诊断授权租户的订单），只是从"预绑定"改为"发现+校验"；证据正文仍只在服务端，客户端只拿到脱敏元数据；仍不替代工程师确认的真实故障业务验收。

### M14：工作区级注册为默认（简化诊断流程）

本次分支：`docs/workspace-level-enrollment-default`。

已完成：

- 文档把"工作区级注册（不传 `--tenant-id`）"设为默认推荐流程：注册后的设备可诊断任意订单，由 `order_snapshot` 按订单号自动发现租户。`--tenant-id` 降级为可选，仅当需把设备限制到单一租户时才传。
- 移除 README、快速上手、gateway 中 `diagnose`/`shell`/`issue-enrollment` 示例里的 `--tenant-id TENANT_ID` / `--tenant-id tenant-a`，改为"只需订单号"。
- 确认适用场景：内部可信运维团队、租户为内部数据分区（非多外部运营商互不可见）。

边界：纯文档变更，代码无需改动（`--tenant-id` 本就可选，`device.tenant_id=None` 等价于不限制，租户发现已在 M13 实现）；工作区级注册意味着已注册运维可查任意租户订单，仅在可信内部团队场景下适用。

### M15：重写打包与下载文档

本次分支：`docs/rewrite-packaging-download`。

已完成：

- 重写 `docs/打包与下载.md` 为步骤式：三种获取方式（① 下载 CI 制品 / ② 服务器中转下载 gh 慢时 / ③ 本机构建）的"选哪条 + 怎么做"，含 `gh run download`、浏览器、服务器中转(Tailscale 临时 HTTP)、本机构建命令和解压后使用。
- 原 M12 版本偏说明性、难理解；改为场景驱动的可复制步骤。

边界：纯文档变更，不改变构建/下载流程或安全边界。

### M16：AFK 工作流脚手架（沙箱运行器 + planner + 标签驱动 Actions）

本次合并：`e3892d1 chore(afk): scaffold AFK development workflow`（由 host runner 合入 main）。

已完成：

- 用 `bootstrap-afk.sh`（python 语言模型）在 AI-Ops 生成 AFK 工作流并合入 main：`.sandcastle/` 沙箱运行器（`main.ts`、`planner.ts`、`profile.ts`、`run-with-retry.ts`、`run-with-extraction.ts` 及各阶段目录 `implement/`、`implement-pr/`、`implement-prd/`、`review/`、`write-pr/`、`write-prd-pr/`、`update-branch/`、`architecture-review/`、`to-issues-prd/`）、`.claude/skills/to-prd-project` 与 `to-issues-project`、`.github/workflows/` 标签驱动 Actions（`implement`、`implement-pr`、`review`、`promote-queued`、`to-issues-prd`、`implement-prd`、`update-branch`、`architecture-review`），以及 `package.json`（`pnpm afk` / `pnpm ralph` / `pnpm prd:to-issues`）、`pnpm-workspace.yaml`（`allowBuilds: esbuild`，规避 pnpm 11 build-script 门禁）和 npm `package-lock.json`。
- 沙箱镜像 `sandcastle:ai-ops`：node 24 基础镜像，携带 claude-code、codex 0.146.1、gh、python3 与 uv；`AFK_PROFILE` 分发封装（`claude` vs `claude-ark|psydo`）与 `AGENT_UID`/`AGENT_GID` 构建参数（= 宿主机 uid/gid）。已核验镜像内含 python 3.11.2、uv 0.12.5，匹配 python 项目自检门 `uv sync --extra dev && uv run pytest && uv run ruff check`。
- 本地验证：`pnpm install`（pnpm 11.15.1）通过，esbuild postinstall 在 `allowBuilds` 下正常执行；`pnpm afk` 无参数触发预期用法守卫、`pnpm ralph` 可加载并启动；新增提交 `pnpm-lock.yaml` 固定 runner 依赖版本。

边界：纯开发工作流脚手架，不改变 AI-Ops 诊断/打包代码、安全边界或用户入口。Actions 在配置 self-hosted runner 与 `AGENT_PAT` 前不生效；当前以 `AFK_PROFILE=<profile> pnpm afk -- <issue>` 本地驱动。

### M17：Java `/diag/order` tracer bullet 团队仓库落地

本次分支：`agent/issue-34-diag-order`（issue #34）。

已完成：

- 新增 `java/README.md` 与两份 Java 源码工件：`DiagQueryController.java`（`GET /diag/order`）和 `DiagQueryAuditAspect.java`（`/diag/*` 审计切面），按 `docs/diag-query-api-plan.md` 落地 T1 模式样板。
- `DiagQueryController` 覆盖：控制器自校验 `X-Internal-Token` + `X-Request-Timestamp`；无效令牌返回 HTTP 401 + `R.failed(401, "令牌无效或过期")`；`order_no`/`tenant_id` 经 `SAFE_VALUE` 白名单（`^[A-Za-z0-9_.:-]{1,128}$`）拦截注入，空白 `tenant_id` 归一为跨租户 `null`；返回 `R<T>`，`data.orders` 为 `ch_order_info` 全字段，`data.fee_template` 包含 `occupy_fee_template` 与 `period_fee_detail`；`ORDER BY created_time DESC LIMIT 3`。
- `DiagQueryAuditAspect` 记录调用方 `internal:AIOps`、按 `@RequestHeader` 排除请求头后的查询参数摘要、返回行数、耗时和成功/失败（HTTP 错误响应记为 failure），不落响应体。
- 新增 `tests/test_diag_order_contract.py`，15 项源码契约测试固定上述外部行为；当时全套测试 224 项通过，`ruff check` 通过。`uv sync --extra dev` 在本仓库 `[dependency-groups]` 配置下不可用，本次实际以 `uv sync --group dev` 安装同一 dev 依赖组后运行测试与静态检查。

边界：本仓库无 JDK/Spring 工具链，未编译、未部署 Java 服务；真实环境验收（`docs/diag-query-api-plan.md` §11 的 Gherkin 场景）为“待验证”。不宣称业务准确率验收或真实故障结论。

### M18：HttpSources 与 fixture 等价测试

本次分支：`agent/issue-36-http-sources-fixture`（issue #36）。

已完成：

- `src/aiops_diagnostics/sources.py` 新增 `HttpSources`，实现 `DiagnosticSources` 六个查询方法，签名与直连源一致，调用 Java 侧 `/diag/*` 固定契约接口并解析 `R<T>` 响应。
- `src/aiops_diagnostics/config.py` 新增 `DiagApiSettings` 与 `AIOPS_DIAG_API_*` 配置，secret 纳入 `redacted()`；`.env.example` 同步新增中文注释模板。
- HMAC-SHA256 内部令牌头 `X-Internal-Token` + `X-Request-Timestamp`；401/403 识别为令牌无效或过期，非成功 `code` 识别为查询拒绝。
- `order_no`、租户、设备和会话号继续走 `_safe_literal` 白名单校验，注入字符不会产生 HTTP 请求。
- `tests/test_http_sources.py` 覆盖端点、查询参数、鉴权头、错误路径、非法 UTF-8 响应、缺配置校验、注入防护和三份 fixture 的 `FixtureSources` vs `HttpSources` 逐字段等价。

验证结果：

- 新增测试 `uv run pytest tests/test_http_sources.py -q` 为 17 项通过。
- 相关回归 `tests/test_sources.py tests/test_config.py tests/test_engine.py` 全部通过；合并后全套 `uv run pytest -q` 为 **241 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。
- 合并验证中修复主线已有缺陷：`_error_detail()` 现在兼容 `HTTPError.read()` 返回 `str` 或 `bytes`，不再因 Python 3.11 下 `HTTPError(..., fp=None)` 返回 `str` 抛出 `AttributeError`。

已知缺口与下一步：

- Java 侧除 `/diag/order` 外其余 `/diag/*` 尚未在本仓库落地，本里程碑只交付 AI-Ops HTTP 客户端和离线二源等价测试。
- `HttpSources` 尚未接入 `live_sources` 诊断入口；切流量、收生产库凭据和真实内网验收留给 Phase 2/3 后续 issue。
- 全部测试仍非生产真实故障验收。

### M19：HybridSources 部分切流量与 fixture 等价测试

本次分支：`agent/issue-50-hybrid-sources-partial-cutover`（issue #50 / PR-A）。

已完成：

- 新增 `HybridSources`：订单/费用/设备/Redis 流走 `HttpSources`，报文与枪次时序走 `TDengineSource`；`live_sources()` 已切换为 `HybridSources`，原直连组合保留为 `direct_sources()`。
- 新增 `aiops_diagnostics.http_auth` 封装 HMAC-SHA256 令牌与鉴权头；HTTP 参数统一走 `_safe_http_param` 白名单。
- `Settings.http` 新增 `base_url` / `internal_token.secret` / `internal_token.expire_seconds`（均 `None` 缺省），保留 `diag_api` 与旧环境变量兼容。
- `tests/test_hybrid_sources.py` 对三份 fixture 做逐字段等价比对，并覆盖 TDengine SQL、doctor 分类与默认源切换。

验证结果：

- `uv sync --group dev`；全套 `uv run pytest` **258 项通过**；`uv run ruff check`、`uv run ruff format --check .`、`uv lock --check`、`uv pip check`、`git diff --check` 全部通过。
- 未改生产 `production.env`、Java/tsdata 仓库或 `docs/diag-query-api-plan.md`。

已知缺口与下一步：

- 报文与枪次时序 HTTP 路径尚未落地，仍直连 TDengine；真实 `/diag/*` 与生产回放未验收。
- `doctor()` 的 mysql/redis 退役分类与 `.env.example` 变量注释留给 PR-B；方案文档同步留给 PR-C。

### M20：方案文档与 AFK 工作流同步到 D 方案

本次分支：`agent/issue-52-plan-and-workflow-sync`（issue #52 / PR-C）。

已完成：

- `docs/diag-query-api-plan.md` 更新为 D 方案已锁定：§1 标注“D 方案（2026-08-27 锁定），TDengine 段暂留直连，Java 侧不动，tsdata 侧不动”；§3 在本轮范围内收窄为订单、设备、Redis 队列、占位费 4 类查询，TDengine 两个查询方法标注“暂不进，等 tsdata 补洞后追加”；§10 改写为“2/3 凭据 → 1/3 凭据 → 0/3 凭据”，Phase 3 拆为 Phase 3a（删 MySQL+Redis）与 Phase 3b（待 tsdata 补洞后删 TDengine）；§12 风险表补充 TDengine 段仍直连与 D 方案不解决 tsdata 自身访问控制两项风险，并同步给 §4 架构图、§6.2/6.3 的 TDengine 接口和 §11.4 收口补充分阶段标注。
- 新建并合并 `docs/afk-cutover/decisions.md`，记录 D 方案决策时间、决策原因、影响 issue 列表，并指向候选团队记忆 `mem-20260827-ranlei-005`（待人工批准；不把候选记忆写成正式结论）。
- `docs/afk-workflow.md` 的 `ready-for-agent → dispatch-only，与 Plan A 一致` 已由 main 上的 PR #48 合入，本次不再重复修改。

验证结果：

- `uv sync --extra dev` 在本仓库失败（`pyproject.toml` 仅定义 `[dependency-groups]`，无 `optional-dependencies`）；实际使用等价命令 `uv sync --group dev` 安装 dev 依赖。
- 与 issue #50 合并后，`uv run pytest` 全套 **258 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。

边界：纯文档变更，无功能变化、无新增自动化检查、无真实环境迁移或收口动作；未完成业务验收。

### M21：内部令牌密钥配置化与 300s 验证窗口

本次分支：`agent/issue-42-internal-token-config`（issue #42 / D 方案下 PR-A 范围）。

已完成：

- `src/aiops_diagnostics/http_auth.py` 新增 `validate_internal_token()`：保持 HMAC-SHA256 + `{timestamp}:{expireSeconds}` 算法，支持新老密钥双 key 过渡；默认验证窗口为 300 秒，与充电桩 Java `InternalTokenManager.expireSeconds` 对齐；空或缺失 secrets 时 fail-closed，不抛内部异常。
- `Settings.http.internal_token` 改为从 `AIOPS_HTTP_INTERNAL_TOKEN_SECRET` 读取密钥、从 `AIOPS_HTTP_INTERNAL_TOKEN_EXPIRE_SECONDS` 读取窗口；未设置时不回退到任何硬编码 secret，窗口未设置时默认 300 秒，同时保留旧 `AIOPS_DIAG_API_TOKEN_SECRET` / `AIOPS_DIAG_API_TOKEN_EXPIRE_SECONDS` 兼容读取。
- `HttpSources` 在构造阶段校验 `内部令牌密钥`，缺密钥时启动即报错，不会发出无鉴权请求。
- `.env.example` 更新为新变量名并注明旧变量兼容方式。
- `tests/test_http_auth.py` 覆盖令牌生成、默认 300s、验证窗口边界与过期、伪造令牌拒绝、非法时钟与有效期、空/缺失 secrets 和双 key 轮换；`tests/test_config.py`、`tests/test_http_sources.py` 覆盖新默认值、旧环境变量兼容与启动期缺配置报错。

验证结果：

- `uv sync --extra dev` 仍因 `pyproject.toml` 未定义 `optional-dependencies.extra=dev` 失败，按仓库既有约束使用等价命令 `uv sync --group dev`。
- `uv run pytest` 全套 **268 项通过**；`uv run ruff check`、`uv run ruff format --check .`、`uv pip check`、`uv lock --check`、`git diff --check` 全部通过。

边界：未连接真实 `/diag/*` 服务，双 key 过渡仅由单元测试固定，不代表生产密钥轮换已经业务验收。

### M22：doctor() 分类与 .env.example 注释化（PR-B）

本次分支：`agent/issue-51-doctor-env-example-partial-cutover`（issue #51 / PR-B）。

已完成：

- `HybridSources.doctor()` 将单次检查拆为 `http` / `tdengine` / `mysql` / `redis` 四段；HTTP 段通过 `/diag/redis-stream` 探针区分 `http.config_missing`、`http.auth_failed`、`http.http_unreachable` 与成功状态，TDengine 段保持只读账号/库存在性断言。
- `mysql` / `redis` 段显式标记为 `deprecated`（`ok=true`，不阻断诊断），并保留 `direct_sources()` 回退能力。
- `render_doctor()` 将 `deprecated` 状态显示为 `DEPRECATED`，HTTP/TDengine 状态继续以 `OK` / `FAIL` 呈现。
- `.env.example` 切换为 `AIOPS_HTTP_BASE_URL` / `AIOPS_HTTP_INTERNAL_TOKEN_SECRET` / `AIOPS_HTTP_INTERNAL_TOKEN_EXPIRE_SECONDS` 新模板；对 MySQL、TDengine、Redis 段增加 PR-B 要求的 `DEPRECATED` / `WARNING` 注释，未修改生产 `production.env`。

验证结果：

- 补充 `tests/test_hybrid_sources.py` 四段分类与 HTTP 失败码测试（含 JSON 响应体 401/403/500 与缺失密钥/有效期场景）、`tests/test_render.py` 的 `DEPRECATED` 渲染测试，并新增 `tests/test_env_example.py` 固定模板注释和新变量。
- `uv sync --group dev`、`uv run pytest` 全套 **268 项通过**，`uv run ruff check`、`uv run ruff format --check .`、`uv lock --check`、`uv pip check`、`git diff --check` 全部通过。
- 任务约定的 `uv sync --extra dev` 在本仓库失败（`Extra dev is not defined in optional-dependencies`），实际使用等价命令 `uv sync --group dev` 安装 dev 依赖；未跳过任何测试或静态检查。

已知缺口与下一步：

- HTTP doctor 探针尚未连接真实 `/diag/*` 服务做内网验收；本里程碑只证明离线分类和模板注释。
- TDengine 段仍未回收凭据，需等待 tsdata 补齐 token 校验后由后续 Phase 3b 收口。
### M23：Java `/diag/occupy-order` 占位费订单查询接口落地

本次分支：`agent/issue-39-diag-occupy-order`（issue #39）。

已完成：

- 在现有 `DiagQueryController` 延续 T1 模式，新增 `GET /diag/occupy-order`：先自校验 `X-Internal-Token` + `X-Request-Timestamp`，无效令牌返回 HTTP 401 + `R.failed(401, "令牌无效或过期")`。
- `order_no` / `order_id` 二选一且必须恰有一个，`order_no` 查 `ch_occupy_order_info.order_no`，`order_id` 查 `ch_occupy_order_info.orderId`，避免混查；`tenant_id` / `status` 为可选过滤，空白值归一为跨租户/不过滤的 `null`。
- 参数沿用 `SAFE_VALUE` 白名单拦截注入；查询返回 `ch_occupy_order_info` 全字段（附录 B.4），`ORDER BY startTime DESC LIMIT 20`，并标记只读事务与查询超时。
- `occupyOrder` 在参数校验后收敛为单一路径执行 `queryForList`，复用已经算出的 `hasOrderNo` / `hasOrderId`，避免重复空值判断与重复查询分支。
- 更新 `java/README.md`，记录 T1/T4 同文件落地边界。
- 新增 `tests/test_diag_occupy_order_contract.py`，14 项源码契约测试固定上述外部行为，并校验参数拦截先于查询、SQL 占位符与绑定参数数量一致。

验证结果：

- `uv sync --extra dev` 在本仓库 `[dependency-groups]` 配置下错误为 Extra `dev` 未定义；实际以 `uv sync --group dev` 安装同一 dev 依赖组后继续检查。
- `uv run pytest` 全套 **292 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。

边界：

- 本仓库仍无 JDK/Spring 工具链，未编译、未部署 Java 服务；`docs/diag-query-api-plan.md` §11 的 Gherkin 场景为“待验证”。
- 本里程碑只落地 Java 查询工件，不新增 `HttpSources` 的占位费方法或真实内网回放；不宣称业务准确率验收或真实故障结论。
### M24：Java `/diag/redis-stream` 同步队列查询接口落地

本次分支：`agent/issue-40-diag-redis-stream`（issue #40，PRD T5）。

已完成：

- 在 `DiagQueryController.java` 新增 `GET /diag/redis-stream`，延续 T1 模式：控制器自校验 `X-Internal-Token` + `X-Request-Timestamp`，无效令牌返回 HTTP 401 + `R.failed(401, "令牌无效或过期")`。
- `stream` 仅放行 `third.order.sync.queue` / `third.order.sync.notify.queue` 两个白名单；非白名单请求返回 HTTP 400 + `R.failed(400, "非白名单 Stream")`，`order_no` 继续经 `SAFE_VALUE` 白名单拦截注入。
- `max_messages` 参数缺省 1000，服务端以 `REDIS_STREAM_MAX_MESSAGES = 1000` 强制 cap，`Math.max(1, Math.min(request, cap))` 拒绝突破上限。
- Redis 只读检查基于 `StringRedisTemplate`：`DataType.STREAM` 判定、`XINFO GROUPS` 消费组、`XLEN` 长度和有界 `XREVRANGE`（`Range.<String>unbounded()` + `Limit.limit().count(limit)`）。
- 响应 `data` 为 `[ { stream, type, length, groups: [{ name, consumers, pending, lag }], inspected_messages, matches } ]`；`order_no` 匹配计数只扫描有界窗口，`lag` 由 Stream 长度与消费组 `lastDeliveredId` 计算。
- 新增 `tests/test_diag_redis_stream_contract.py`，16 项源码契约测试固定上述外部行为，覆盖 `max_messages` 非正值保护、缺失 Key 与畸形 `lastDeliveredId` 的空值防护。

验证结果：

- 聚焦测试 `uv run pytest tests/test_diag_redis_stream_contract.py tests/test_diag_order_contract.py -q` 通过（16 + 15 项）。
- 全套 `uv run pytest` 为 **257 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。
- 任务约定命令 `uv sync --extra dev` 在本仓库失败（`Extra dev is not defined in the optional-dependencies table`），因为 `pyproject.toml` 将开发依赖声明在 `[dependency-groups]`；本次实际执行等价命令 `uv sync --group dev`，未跳过测试或静态检查。

边界：本仓库无 JDK/Spring 工具链，Java 工件未编译、未部署，未对真实 `cloud-charging-pile-web` 执行 `docs/diag-query-api-plan.md` §11 的 Redis Gherkin 场景；“通过”只代表源码契约自动化检查，不代表真实故障或业务准确率已经验收。
### M25：Java `/diag/device` 设备信息查询落地

本次分支：`agent/issue-41-diag-device`（issue #41）。

已完成：

- 在 T1 的 `DiagQueryController.java` 落地 `GET /diag/device`，沿用同一模式：控制器自校验 `X-Internal-Token` + `X-Request-Timestamp`；`device_id` / `device_code` 二选一必填、非法参数返回 HTTP 400 + `R.failed(400, "非法参数")`；两个值均进入 `SAFE_VALUE` 白名单后才执行查询；`tenant_id` 空值归一为交叉租户 `null`。二选一守卫用 `hasDeviceId` / `hasDeviceCode` 布尔判断表达，避免双 null 比较的可读性歧义。
- 查询 `iot_charging_device` 的十字段快照（id/tenant_id/site_id/device_code/protocol/online_status/status/work_status/error_reason/fee_template_id），按 id 或 device_code 二选一过滤、`tenant_id` 可选过滤、`LIMIT 1`，使用参数化 SQL 和 `@Transactional(readOnly = true, timeout = 5)`。
- 未命中时 `data` 返回 `null`，保持 `docs/diag-query-api-plan.md` §6.6 的契约。
- 新增 `tests/test_diag_device_contract.py` 的 16 项源码契约测试，固定路由、令牌自校验、`R<T>` 响应、设备字段、二选一必填、注入拦截顺序、有界参数化查询、两种 SQL 字段与占位符一致性、未命中 `null`、空白键归一顺序、租户键校验顺序、查询 SQL 分支选择以及审计单对象/数组行数。
- 复查发现 `DiagQueryAuditAspect.rowsOf` 对 `/diag/device` 返回的单个对象会记为 `rows=0`，不符合 §8 的“返回行数”语义；已改为数组按元素计数、非空对象按 1 行计数，并补充契约测试固定该行为。

验证结果：

- 在 `pyproject.toml` 新增 `[project.optional-dependencies] dev`，与既有 `[dependency-groups] dev` 保持同一依赖清单，使任务约定命令 `uv sync --extra dev` 可直接通过，同时保留 `uv sync --dev` / `uv sync --group dev` 兼容。
- 任务约定命令 `uv sync --extra dev && uv run pytest && uv run ruff check` 直接通过；合并后 `uv run pytest` 全套 **324 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。

边界：本仓库无 JDK/Spring 工具链，未编译、未部署 Java 服务；真实环境验收（`docs/diag-query-api-plan.md` §11 的 Gherkin 场景）为“待验证”。不宣称业务准确率验收或真实故障结论。

### M26：Phase 3a 收口 —— 删 MySQL+Redis 直连凭据，TDengine 留口子

本次分支：`agent/issue-43-remove-mysql-redis-credentials-phase-3a`（issue #43 / PR-D，Phase 3a）。

已完成：

- 新增 `docs/afk-cutover/pr-d-spec.md`，固定 Phase 3a 的范围、验收、回滚预案与未完成边界；生产 MySQL / Redis 凭据删除前必须先备份 `production.env` 到仓库外路径。
- `.env.example` 不再提供 `AIOPS_MYSQL_*`、`AIOPS_REDIS_*`、`AIOPS_SSH_MYSQL_*`、`AIOPS_SSH_REDIS_*` 的活动配置；MySQL / Redis 段改为注释化的 `REMOVED (Phase 3a)`，TDengine 直连与 SSH 隧道配置保留。
- `HybridSources.doctor()` 的 MySQL / Redis 段文案改为“已收口”，不再声称生产可直接 fallback 到本仓直连；`live_sources()` 继续默认 `HybridSources`，`direct_sources()` 仅作为回退保留。
- `live_sources()` 的 SSH 隧道只建立 TDengine 转发，不再请求 MySQL / Redis 转发；`direct_sources()` 仍按回滚路径建立三条转发。
- README、架构、Gateway、快速上手和 `ops/README.md` 同步为：订单/费用/设备/Redis 队列经 `/diag/*` HTTP，TDengine 直连代理仍保留。

验证结果：

- 新增 `tests/test_env_example.py` 的 Phase 3a 模板断言，固定 `.env.example` 不含活动 MySQL / Redis 直连配置，同时保留 TDengine 与 HTTP 配置。
- `tests/test_hybrid_sources.py` 新增 SSH 隧道回归测试，固定混合路径只转发 TDengine、直接回退路径保留三条转发。
- 任务约定命令 `uv sync --extra dev && uv run pytest && uv run ruff check` 直接通过；`uv run pytest` 全套 **327 项通过**，`uv run ruff check` 通过，`git diff --check` 通过。

边界：本仓库未直接修改生产主机上的私有 `production.env`；真实 provider 的 `aiops-gateway` fixture 端到端冒烟、真实 `/diag/*` 连通和生产凭据删除结果均为“待验证”。未完成业务验收。


## 当前缺口与下一步

1. 由用户在 Windows 本机运行最终 ZIP，重点反馈首次注册、系统凭据库、`remote doctor`、run/event 显示、中文输出和杀毒软件兼容问题。
2. 若用户环境与 runner 不同，重新执行源码外解压、`agent-doctor`、fixture 验证和进度/事件日志检查。
3. 获取并记录工程师确认的真实故障案例，按 YKC 金额、交易数据缺失、状态 2、状态 5、OCPP 计费和 Redis 同步路径分别验收。
4. 正式部署前完成 OIDC Device Flow/mTLS、Vault/KMS、PostgreSQL、速率限制、审计留存、备份恢复和服务级监控。
5. 企业分发前补充 Windows 代码签名、制品保留和升级策略；当前合成 fixture 仍不代表生产准确率。
6. 在 GitHub 仓库配置 `AFK_PROFILE` Actions 变量（`claude` / `claude-ark` / `psydo` / `aliyun-deepseek`）、`AGENT_PAT` secret 与 `agent:implement` 等标签；部署 self-hosted runner 后标签驱动 Actions 才生效。
7. 首次真实运行需在 `.sandcastle/.env` 配置 provider 凭据（或用 `--profile`），并选一个开放 issue 做端到端验证。

### M27：AFK pull_request_target 可信控制面加固

本次分支：`fix/harden-afk-review`。

已完成：

- 升级 afk-bootstrap 模板到 1.1.1；PR 变更工作流仅接受仓库所有者创建的同仓库 PR。
- 宿主依赖与 Sandcastle 编排从当前 `main` controller 加载；候选分支只在带只读 token 的 Docker 沙箱执行；结果经 Git bundle 导入干净 delivery checkout，并拒绝远端 head 竞态。
- `AGENT_PAT` 缺失或交付失败时 fail closed 并记录 `agent:blocked`，不再回退到不能触发下游 workflow 的 `GITHUB_TOKEN`。
- 新增 ADR、Gherkin、QA 计划和 deterministic `AFK Policy` workflow；仓库 checker 静态锁定关键安全不变量，模板 shell 测试覆盖 stale main、提交保留和竞态拒绝。

验证边界：AFK-B10/B11 已在 `qa-plan.md` 记录提交、环境、时间戳和命令证据；AFK-B12 必须等待加固 workflow 合并后，在在线 self-hosted runner 上完成 owner-authored `agent:review` canary。该变更不修改 AI-Ops 诊断 API、数据库、业务安全边界或用户入口。

## 更新规则

每次里程碑提交前必须同时检查：

- `docs/开发进度.md` 是否记录本次目标、提交、验证、缺口和下一步。
- `docs/validation.md` 是否记录新增测试、真实环境结果和失败修复。
- README、部署和架构文档是否与用户入口和安全边界一致。
- 所有“通过”“完成”“验收”结论是否能在 CI、artifact、测试输出或工程师确认记录中找到证据。

### M28：AFK 工程经济契约升级到 1.1.2

本次分支：`fix/economy-contract`。

已完成：

- 将 AFK 模板升级到 1.1.2，在共享 `CODING_STANDARDS.md` 中加入
  provider-neutral 的 Economy ladder、根因定位、依赖选择、兼容性边界、深模块、
  最小端到端切片和长期架构约束。
- 所有会修改代码的实现、PR 反馈修复和 review prompt 都引用同一契约；未在 Docker
  镜像或 Claude/Codex 私有目录安装 Ponytail skill。
- 公开接口、schema、持久化数据和生产行为仍按兼容性契约处理；只有无消费者的内部或
  实验路径才默认删除过时实现，不新增掩盖失败的 fallback。

验证结果：

- `uv sync --extra dev`、全套 `uv run pytest` **327 项通过**、
  `uv run ruff check`、`uv run ruff format --check .`、`uv lock --check`、
  `uv pip check`、AFK policy、review controller 语法检查和 `git diff --check`
  全部通过。

边界：本次只改变 AFK 开发治理文本和 review 指令，不修改诊断 API、数据库、权限、
生产配置或业务逻辑；未执行真实故障案例，因此未完成业务验收。Dockerfile 和 agent
CLI 工具链未变，无需重建镜像。

### M29：T1 权限上下文与身份映射落地

本次分支：`agent/issue-71-scope-context-identity-mapping`（issue #71，PRD #23 T1）。

已完成：

- 新增 `src/aiops_diagnostics/scope_context.py`：单次诊断运行专用的不可变
  `ScopeContext`（frozen dataclass），显式区分调用者、目标主体、B 端
  `SysUser.id`、绑定的 C 端 `SysUser.userId`、有效租户、组织/店铺/站点、业务
  数据范围、调用者有效角色（含继承）和能力权限，并生成确定性 `scope_fingerprint`
  （校验不一致即拒绝），审计面 `audit_summary()` 只暴露摘要与指纹，不含凭证或
  权限副本。
- `ScopeResolver` 从平台凭证 + 可选 B 端用户 ID / C 端用户 ID / 租户条件解析上下文：
  凭证为空、主体不存在、C 端映射歧义、同时提供两种目标 ID、越权代查、租户越权、
  范围为空或租户无法确定均以带错误码的 `ScopeError` fail closed；普通调用者的
  有效租户必须等于其自身租户，平台管理员角色（含继承）才能显式切换租户或跨租户
  代查。解析器不持有任何业务数据源，失败路径不会触发数据库查询。
- 平台依赖收敛到 `PlatformDirectory` 单一接缝，并提供 `UpmsDirectory` HTTP 实现：
  调用者凭证以 `Authorization: Bearer` 透传 `/user/info`、`/user/ds`、
  `/user/inside/byId/{id}`、`/user/inside/byUserId/{userId}`；端点路径与响应字段按
  PRD #23 记录的 cloud-upms 能力固定并做严格解析，形状无法识别、UPMS 不可达或
  凭证被拒绝时 fail closed；用户 ID 进入 URL 前经白名单校验，错误消息不含凭证。
  Dis 点位到站点的归属解析将由 T2/T3 在同一接缝加入。
- 代查/管理权限码与管理角色码通过 `ScopePolicy` 显式传入，默认空集合（fail
  closed）；平台真实权限码由后续接入任务写入配置（见 `ponytail` 标注）。
- 新增 `UpmsSettings`（`AIOPS_UPMS_BASE_URL`、`AIOPS_UPMS_TIMEOUT_SECONDS`），
  校验地址与超时边界；`.env.example` 增加 UPMS 段并说明调用者凭证只在运维入口
  透传、不在配置文件出现。

验证结果：

- 新增 `tests/test_scope_context.py`（29 项）与 `tests/test_scope_context_http.py`
  （12 项）：覆盖有效主体、未知主体、C 端映射歧义、越权目标、租户越权、平台管理
  员租户切换、空范围、self/all 范围类型、角色继承、依赖服务失败、凭证为空、UPMS
  HTTP 不可达/401/拒绝响应/畸形响应、路径注入拦截、不可变性、指纹确定性与防篡改、
  审计摘要不含凭证和权限副本。
- `tests/test_config.py`、`tests/test_env_example.py` 增加 UPMS 配置与模板断言。
- 任务约定命令 `uv sync --extra dev && uv run pytest && uv run ruff check` 通过；
  全套 **371 项通过**，`uv run ruff format --check .` 通过，`git diff --check`
  通过，`node .sandcastle/policy-check.mjs commit` 通过。

边界：UPMS 端点路径与响应字段契约只有离线契约测试守护，未对真实 `cloud-upms`
执行调用；`ScopePolicy` 的平台真实权限码/角色码尚未配置，因此生产接入前代查与
租户切换保持关闭。`ScopeContext` 尚未接入 Gateway/CLI 诊断路径（T5）、MySQL/
TDengine/Redis 受限查询（T2/T3/T4）与证据审计落盘（T5）；未修改任何 Java 仓库，
未删除生产凭据，未执行真实流量切换。

### M30：T2 MySQL 受限查询（ScopeContext 下推）

本次分支：`feat/issue-74-scoped-mysql-queries`（issue #74，PRD #23 T2）。

已完成：

- 新增 `src/aiops_diagnostics/query_scope.py`：把 T1 的 `ScopeContext` 解析为不可变
  `QueryScope`（有效租户 + 站点集合 + self 用户过滤）。站点集合由 UPMS 数据范围
  `siteIds`、店铺 `shopIds → ch_site` 归属、代查目标的 Dis 点位归属（
  `/dis/admin/device/point/{uid}/device/points`）解析并取交集；`all` 范围不限站点，
  `self` 范围按主体 C 端用户过滤；任一依赖失败、配置缺失或范围 ID 超限时带错误码
  fail closed，不触发 MySQL 查询。
- 新增 `DisHttpDirectory` 与 `DisSettings`（`AIOPS_DIS_BASE_URL` /
  `AIOPS_DIS_TOKEN` / `AIOPS_DIS_TIMEOUT_SECONDS`）：复用充电桩 Java
  `DisFeignInterceptor` 契约（Authorization 服务侧静态令牌 + tenantId/site/
  saasType）；令牌纳入 `redacted()`，不入审计或错误消息。
- `MySQLSource` 支持可选 `scope` 参数：订单/占位费订单/设备查询把租户、站点、
  self 用户映射为 SQL 过滤条件（参数绑定），忽略调用方传入的裸 `tenant_id`；
  站点范围为空时短路线返回空证据，不发起 SQL；计费模板查询先以订单范围做存在性
  检查，保证单独查询不扩大可见范围。
- 新增占位费订单查询 `get_occupy_orders`（`orderId`/`order_no` 二选一，与
  `OccupyOrderTxDataHandler` 关联键一致）及 `site_ids_by_shops` /
  `site_ids_by_points` 站点归属解析（只读、参数绑定、LIMIT 1000）。

验证结果：

- 新增 `tests/test_query_scope.py`（17 项）与 `tests/test_mysql_scope.py`（16 项）：
  范围解析（organ/all/self、店铺展开、Dis 交集、空范围、超限 fail closed）、MySQL
  scope 下推（租户/站点/用户、忽略裸 tenant、空站点短路、fee 存在性检查、占位费
  关联键、设备站点过滤）、Dis HTTP 契约（令牌/租户头、401/不可达/畸形 fail
  closed、路径注入拦截）、配置脱敏与模板断言。
- `uv sync --extra dev`、全套 `uv run pytest` **413 项通过**、`uv run ruff check`、
  `uv run ruff format --check`、`compileall`、`uv pip check`、`uv lock --check`、
  `git diff --check`、`node .sandcastle/policy-check.mjs commit` 全部通过。

边界：Dis 端点与响应字段契约按 Java `DisFeignClient` 固定并由离线测试守护，未调用
真实 Dis；站点归属解析与范围下推尚未接入 Gateway/CLI 诊断路径（T5）与 TDengine/
Redis 受限查询（T3/T4）；未修改任何 Java 仓库，未删除生产凭据，未执行流量切换。

### M31：T3 TDengine 受限查询（设备集合约束）

本次分支：`feat/issue-73-scoped-tdengine-queries`（issue #73，PRD #23 T3）。

已完成：

- `TDengineSource` 支持可选 `allowed_devices`（frozenset）：每个枪属性/报文查询
  的 `device` 必须在该集合内，否则以 `ScopeError(code=tdengine.device_forbidden)`
  拒绝且不发起 TDengine 请求；`None` 表示由上游订单 scope 已约束设备，但仍走
  固定超表/固定字段/强制时间窗口/服务端 LIMIT 构造，不接受客户端透传表名、字段
  或 SQL。
- `HybridSources` 与 `live_sources()` 透传 `allowed_devices` 到 TDengine 源，
  为 T5 运行时接入做准备。
- 设备标识继续经 `_safe_literal` 白名单拦截注入；枪属性与报文查询保持
  `charging-gun_property` / `charging-pile_comm` 固定超表与固定字段。

验证结果：

- 新增 `tests/test_tdengine_scope.py`（7 项）：允许设备、越权设备拒绝且不发起
  请求、无 allowed 集合时允许任意安全设备、固定超表/字段/时间窗/LIMIT、注入拦截。
- `uv sync --extra dev`、全套 `uv run pytest`（414 项通过，较 main 407 新增 7）、
  `uv run ruff check`、`uv run ruff format --check`、`compileall`、`uv pip check`、
  `uv lock --check`、`git diff --check`、`node .sandcastle/policy-check.mjs commit`
  全部通过。

边界：TDengine 设备集合校验尚未接入诊断运行时（T5 按已解析设备集合注入）；未
修改任何 Java 仓库，未删除生产凭据，未执行流量切换。

### M32：T4 Redis Stream 受限查询（租户归属验证）

本次分支：`feat/issue-72-scoped-redis-queries`（issue #72，PRD #23 T4）。

已完成：

- `RedisSource` 支持可选 `order_in_scope` 归属验证谓词：只有谓词判定为 True
  的消息才被计为该订单的匹配；无法验证租户/站点/订单归属的消息不计数，且保持
  只返回元数据（Stream 长度、消费组、`inspected_messages`、`matches`），不返回
  原始消息正文。
- 新增 `make_redis_order_in_scope(scope)`：从 `QueryScope` 构造租户归属谓词，
  校验消息字段 `tenantId`/`tenant_id`（str/bytes）与有效租户一致。
- 保持白名单 Stream 集合（`third.order.sync.queue` / `third.order.sync.notify.queue`）、
  有界 `XREVRANGE`（`redis_max_messages`）、`XINFO GROUPS` 与服务端消息上限；
  非 Stream 类型安全返回空证据。

验证结果：

- 新增 `tests/test_redis_scope.py`（9 项）：租户命中计数、跨租户排除、无法验证
  租户排除、无 scope 谓词按订单号匹配、有界读取与白名单、不返回原始正文、
  非 Stream 安全空证据、谓词单元断言。
- `uv sync --extra dev`、全套 `uv run pytest`（416 项通过）、`uv run ruff check`、
  `uv run ruff format --check`、`compileall`、`uv pip check`、`uv lock --check`、
  `git diff --check`、`node .sandcastle/policy-check.mjs commit` 全部通过。

边界：Redis 归属谓词尚未接入诊断运行时（T5 注入 order_in_scope）；未修改任何
Java 仓库，未删除生产凭据，未执行流量切换。

### M33：T5 运行时集成、审计与旧路径清理

本次分支：`feat/issue-75-scope-runtime-integration`（issue #75，PRD #23 T5）。

已完成：

- 新增 `ScopedSources`（受限直连诊断源）与 `DeviceGate`（订单元数据设备集合）：
  MySQL 按 `QueryScope` 下推租户/站点/用户；TDengine 只允许订单元数据里的设备
  （初始空集合 fail closed，seed 后生效）；Redis 只统计租户归属可验证的消息。
  任一环节缺失不退回无范围直连。
- 新增 `scoped_live_sources(settings, scope=...)` 工厂，与既有 `live_sources`
  平行；`run_agent_diagnosis` 与 CLI 确定性路径在提供 `QueryScope` 时切换受限源。
- CLI `diagnose` 新增 `--scope-json`（`QueryScope` JSON），由已认证运维入口透传；
  解析失败即拒绝，本工具不信任前端裸传权限字段。
- 新增 `tests/test_scope_runtime.py`（8 项）：DeviceGate 收集、受限源组装、
  越权设备拒绝、scope 下推、scoped_live_sources 构造。
- 补齐 `acceptance.feature`（权限上下文/受限查询/审计 Rule 与 Scenario）与
  `qa-plan.md`（SCP-01..05 可执行 QA 用例），覆盖有效/无效凭证、目标主体越权、
  租户切换、UPMS/Dis 不可用、三类数据库受限查询与审计结果。

验证结果：

- `uv sync --extra dev`、全套 `uv run pytest`（431 项通过，较 main 414 新增 17）、
  `uv run ruff check`、`uv run ruff format --check`、`compileall`、`uv pip check`、
  `uv lock --check`、`git diff --check`、`node .sandcastle/policy-check.mjs commit`
  全部通过。

边界：`--scope-json` 由上层已认证入口透传，本仓库不校验平台令牌（网关职责）；
`ScopedSources` 直连接线由离线测试守护，未连接真实生产库；未修改任何 Java 仓库，
未删除生产凭据，未执行真实流量切换。真实环境验收按 `qa-plan.md` SCP 用例执行。

### M34：生产 `/user/ds` 平台缺陷的我侧降级修复

本次分支：`feat/issue-75-ds-scope-fallback`（PRD #23 真实验收收尾）。

已完成：

- 定位生产缺陷根因：`cloud-upms-admin` 的 `SysUserController.getDs:1023` 调
  `SysOrganMapper.getBizData` 时组织 ID 列表为空，生成非法
  `select biz_data from sys_organ where id in`（空 IN 子句），Druid
  `merge sql error` → 所有用户 500；且 `sys_organ.biz_data` 18 行全为 NULL。
- `UpmsDirectory.data_scope()` 增加降级推导（不改任何 Java 仓库）：
  `/user/ds` 服务侧错误时改走 `/role/list`（取调用者角色最宽 `dsType`，
  平台 `DataScopeTypeEnum`：0=全部/1=自定义/2=本级及子级/3=本级）+
  `dsScope`（自定义组织集合）+ 调用者 `organId`；店铺范围走
  `/shopuser/getShops`。凭证失败仍直接 fail closed，角色不可识别/
  `dsType` 未知/响应形状非法均按 `scope.upms_unavailable` fail closed。
- 新增 `tests/test_scope_context_ds_fallback.py`（11 项）：降级推导（all/
  本级及子级/自定义/多角色取最宽/忽略非本人角色）、凭证失败不降级、
  无角色/未知类型/非法形状/不可达保持 fail closed、成功路径不受影响。

验证结果：

- 全套 `uv run pytest`（442 项通过）、`uv run ruff check`、
  `uv run ruff format --check`、`compileall`、`uv pip check`、
  `uv lock --check`、`git diff --check`、`node .sandcastle/policy-check.mjs commit`
  全部通过。
- 真实生产验证（经 124 内网隧道）：`/user/ds` 500 时 `testadmin` 端到端
  `ScopeResolver → QueryScope` 成功；角色 `dsType=0` → `DataScope(all)`，
  忠实反映平台角色配置，未扩大平台授予的范围。

边界：平台 `/user/ds` 缺陷本身仍需平台侧修复；本侧降级保证验收不阻塞且不扩大
访问；`testadmin` 角色为"全部"范围，组织/店铺粒度降级行为由契约测试守护。

### M35：标准调用者认证与订单授权 tracer bullet

本次分支：`feat/standard-caller-auth`（issue #86，PRD #84 T1）。

已完成：

- Gateway 新增消费端无关的标准 Bearer 认证接缝 `CallerContextResolver`，资源接口不
  感知 Web、移动端、BFF 或具体登录态；既有 `aops_*` enroll/run 接口保持原语义。
- 新增 OAuth 2.0 introspection 适配器，验证 active、subject、tenant、AI-Ops
  audience、expiry、required scope 和业务数据范围，生成不可变 `ScopeContext`；
  网络或响应异常 fail closed，错误不包含 token/client secret。
- 新增标准订单授权探针 `GET /v1/orders/{order_no}/access`，复用
  `ScopeContext → QueryScope → ScopedSources`；范围外和不存在订单统一 404，避免
  泄露资源存在性。订单号在数据源之前走固定白名单。
- 标准接口使用稳定 `error.code/message/retryable`，与旧 FastAPI `detail` 合同隔离。
- 补充标准认证配置模板、Gateway 部署说明、Gherkin 场景与 SAPI-01..05 QA 计划。

验证结果：

- 新增 14 项认证/标准 API 测试；全套 **456 项 pytest 通过**。
- Ruff、格式、compileall、依赖检查、锁文件检查、policy check 与 `git diff --check`
  通过。

边界：当前提供可运行的 introspection 适配器；JWT 验签仅固定为可插拔 resolver
边界，仓库尚未批准或引入 JOSE 依赖，因此没有手写不安全的 JWT 实现。未连接真实
issuer、introspection 或生产订单，**未完成业务验收**；真实环境验证留给 issue #92。

### M36：最小异步充电健康报告作业

本次分支：`feat/restore-async-health-report-jobs`（issue #87，PRD #84 T2）。

已完成：

- 标准 Bearer 调用方可为授权且已结束的订单创建健康报告作业，并通过 opaque
  `job_id` 轮询 `queued/running/completed/failed/expired` 状态。
- Gateway SQLite/WAL 新增短生命周期作业记录：同 scope fingerprint、订单和
  `rule_version` 复用活动或 15 分钟内完成结果；失败保留 5 分钟；服务启动时遗留
  queued/running 作业转 expired，终态不可被迟到 worker 覆盖。
- 复用现有 Gateway executor 和 `ScopedSources` 执行报告，不引入 Celery、消息队列
  或独立 worker；30 秒 deadline 超时后 fail closed。
- 新增确定性最小报告：验证订单已结束、设备和时间窗口，输出停止原因指标、摘要、
  completeness、`rule_version=health-v1` 和 `data_as_of`；停止原因缺失时逐项
  unavailable，不使整个报告失败。
- API 在创建前重新校验订单授权；不同调用者 scope、随机 job ID 和无权订单统一
  404，不返回 scope fingerprint、VIN、SQL、原始报文或内部路径。
- 补齐 Gherkin 场景和 HRJ-01..05 QA 计划。

验证结果：

- 新增 15 项健康报告、作业状态和 API 合同测试；全套 **471 项 pytest 通过**。
- 恢复审查补强 deadline：任何状态写入前先过期超时作业，迟到 worker 不能在 30 秒
  deadline 后把 expired 作业覆盖为 completed。
- 原实现曾在 PR #94 通过 Linux verify、Windows verify、Workflow policy 和
  CodeRabbit minimal-risk review；其后因会话接续治理回滚由 PR #98 撤销，并非缺陷
  回滚。本分支从该已验证 squash commit 恢复相同代码，再补齐强制里程碑文档。
- Ruff、格式、compileall、依赖检查、锁文件、policy check 与 `git diff --check`
  重新验证通过后方可交付。

边界：当前报告仅包含一个停止原因指标，完整指标和曲线分别由 issue #90/#89 交付；
所有订单与 caller 均为离线 fake，**未完成业务验收**，真实环境验证留给 issue #92。

### M37：标准单问诊断与主体级历史查询

本次分支：`feat/restore-standard-diagnosis-api`（issue #88，PRD #84 T5）。

已完成：标准接口支持自由文本和指标上下文单问诊断、主体 scope 隔离的历史列表与详情；请求拒绝客户端伪造报告字段，runtime 调用现有 `run_agent_diagnosis(scope=...)`，内部 run ID 仅服务端保存。诊断状态覆盖 queued/running/completed/inconclusive/failed/expired，完成/不确定结果保留 15 分钟，失败 5 分钟，终态不可被迟到 worker 覆盖。

诊断资源要求独立 `aiops:diagnoses:write` scope；仅有订单读取权限的调用者不能创建、查询或列出诊断。

边界：真实 provider、issuer、生产订单和故障准确率验收留给 #92。
### M38：标准健康报告曲线与来源摘要

本次分支：`feat/standard-health-curves`（issue #89，PRD #84 T4）。

已完成：新增消费端无关曲线模块，按完整受限时序数据生成功率、电压、温度曲线；每条曲线最多 300 个时间有序点并保留首点、末点和极值；遥测缺失时报告部分完成并在来源摘要标记 unavailable。当前电压曲线只使用已确认的 `outputVoltage`，未确认 BMS 字段不猜测。

边界：完整健康指标由 #90 交付，真实 TDengine 验收留给 #92。
### M39：完整充电健康指标与部分成功语义

本次分支：`feat/complete-health-metrics`（issue #90，PRD #84 T3）。

已完成：新增确定性温度、最高温、电池容量、电压均衡、SOC 均衡评分和 SOH 计算；SOC 使用百分点，SOH 仅接受权威标称容量；缺少业务定义、输入或容量时返回 unavailable，不使用模糊车型匹配；接入健康报告 radar、health_metrics 与 rule_version。

边界：协议告警码和部分字段仍按 #85 未确认语义保持不可用；真实 TDengine/车辆档案验收留给 #92。
### M40：统一标准 API 契约

本次分支：`feat/unified-standard-api-contract`（issue #91，PRD #84 T6）。

已完成：统一健康报告作业与单问诊断的 Bearer 认证、稳定错误形状、opaque 资源 ID、轮询字段和敏感字段黑名单；两类资源保持独立状态与权限。补齐合同验收和 CONTRACT-01 QA。
### M41：标准 API 真实环境验收准备

本次分支：`docs/standard-api-real-qa`（issue #92，PRD #84 T8）。

已完成：把真实验收拆为 REAL-API-01..04，覆盖完整订单、部分数据、越权隔离、诊断性能和重启语义；明确执行环境、前置、数据、动作、预期、清理和证据要求。

当前状态：REAL-API-01..04 均为 **blocked**。当前没有批准的标准 API issuer/introspection、调用方 access token、部署入口和本 PRD 可用真实订单；已有 `docs/qa-plan-real.md` 仅覆盖 PRD #23 受限直连，不能替代本次验收。未完成业务验收。
### M42：标准 API 生产结构仿真验收数据

本次分支：`feat/synthetic-real-acceptance-data`。

已完成：新增完全由 `SYNTH-*` 标识组成的生产结构仿真数据集和标准库 runner，覆盖完整 400 点订单、无遥测部分订单、跨租户越权订单；runner 已验证曲线 300 点上限、来源不可用和统一拒绝语义。

验证：runner 通过；全套 pytest、Ruff、compileall、依赖、锁文件、policy check 和 diff check 通过。数据不含生产订单、凭据或 PII。

边界：这是离线系统验收输入，不是生产真实数据；REAL-API-01..04 仍因缺少批准 issuer、调用方 token、部署入口和真实订单而 blocked。
### M44：C 端 thirdSession Redis 适配与真实样本核验（2026-09-03）

AI-Ops 已支持服务 Bearer + `X-Third-Session`，读取真实业务 Redis 的
`app:3rd_session:` 会话并复用 `ScopeContext`；Java 仓库未修改。已建立最小 ACL 和
`127.0.0.1:16389` 用户级隧道。截图 token 经只读验证不存在，真实验收仍待当前有效会话。

### M43：公司 cloud-auth Bearer 认证接线

本次分支：`feat/cloud-auth-bearer-resolver`。

已完成：标准 API 在未配置 OAuth introspection 时默认使用现有 `cloud-auth -> UPMS` Bearer 验证链，复用 `ScopeResolver/UpmsDirectory` 生成不可变 `ScopeContext`；不复制 HS256 签名密钥，不接受裸 user_id/tenant_id。补齐 cloud-auth 验收场景和 QA 计划。

验证：认证/标准 API 回归通过；真实 token 验证和真实订单验收待公网服务重启与 #92。


## 2026-09-06 服务迁移至公司 120（issue #147）

生产 AI-Ops 网关迁至公司 120（systemd `aiops-gateway.service`，127.0.0.1:8788，开机自启），`api.qumall.qushiyun.com/v1/*` 同机反代；数据面内网直连（MySQL 0.39 / 本机 Redis / TDengine / UPMS 5999），0 SSH 隧道。原服务器网关与隧道单元停用留冷备，FRP 域名留作回滚入口。S1/S2/S3 验收全绿（FAQ/健康报告线可用；诊断执行面通过，终态复验待 9-21 模型配额重置）。上节 ranlei 域名为迁移前历史状态。

## 2026-09-09：T2 智能体草稿、发布、停用与版本快照（issue #169）

本次分支：`feat/agent-lifecycle`。

已完成：新增租户隔离的 `AgentStore`/`AgentManager` 和 `/v1/agents` 管理 API。草稿支持客服/运维类型、业务 Prompt、知识库绑定、开场问题、快捷指令、模型和输出合同；保存使用 revision 乐观并发；发布前校验模型 allowlist、输出合同和注入式知识库状态 resolver，成功后写入不可变 version snapshot；已发布版本可派生新草稿并递增版本；停用禁止后续生命周期动作但保留历史版本；仅未发布草稿可删除。API 需要 `aiops:agents:manage` Bearer 接缝和 agent 管理角色，跨租户/不存在统一 404。

验证结果：`tests/test_agent_lifecycle.py` 4 项通过；T1 合并后完整 pytest 554 项通过；Ruff、格式、compileall、依赖、锁文件和 diff check 通过。

已知缺口：当前仅完成 AI-Ops 生命周期协议和离线 fake 验证，真实 UPMS 角色、知识库状态、Java 管理 BFF 和 `qumall-admin` 页面留给 #171/#170；**未完成业务验收**。下一步为 T3 QA RAG 单轮运行接入 #170。

## 2026-09-10：T1/T2 合并后本地集成验收

T1 已由 PR #176 合并为 `c8be858`；T2 分支已基于该 `origin/main` 完成回归。T1 媒体协议、T2 智能体生命周期、统一问答 API、QA store 和 Gateway API 专项 31 项通过；全量 pytest 554 项通过，Ruff、格式、compileall、依赖、锁文件和 diff check 全部通过。

验收边界：这是本地 AI-Ops 集成验证。#170 QA RAG 运行时接线、Java BFF、`qumall-admin` 浏览器、真实 `kb-service/RAGFlow` 和图片/视频知识库数据尚未接入，因此**完整业务端到端验收未完成**。

## 2026-09-10：kb-service 测试环境事实文档（issue #170 前置）

把此前只存在于会话与 Playground 的 kb-service 事实落进仓库：`docs/agents/kb-service-test-env.md` 记录服务拓扑（admin 域名 /kb 路由 → gateway → kb-service → RAGFlow v0.27.1，全回环）、联调鉴权两条路线（AI-Ops 走路线 B 手动 tenant-id + 懒注册，canary 固定 `aiops-canary` 租户）、检索协议样例（`POST /kb/knowledge-bases/{kb}/search`，字段映射按 RAGFlow 源码核实）、媒体读取路径结论（图片需 kb-service 补 `/documents/images/{image_id}` 透传端点；视频走已有 download 端点 + AI-Ops 媒体代理实现 Range）、canary 纪律与业务方联调素材位置（`docs/知识库材料/`，gitignored）。

关键现场事实：120 的 RAGFlow 5 容器与 kb-service 于 09-09 15:13 被人为停机（内存紧张背景），公网 `/kb/healthz` 当前 502；T1 mock-first 模式下 #170 不依赖活栈，重启 KB 栈与补图片端点列为人工决策点，不由 agent 擅自操作。`.gitignore` 同时加入知识库素材与 netcatty 残留排除。

## 2026-09-10：T3 客服 QA RAG 单轮运行接入统一入口（issue #170）

本次分支：`feat/qa-rag-runtime`。

已完成：`qa` 路径接线已发布客服智能体。`qa_rag.py` 提供 `select_customer_agent`（仅已发布、绑知识库的 customer 智能体入选，租户隔离无存在性泄露）与 `run_customer_qa_answer`（Codex harness `tool_requests → 执行 → 同 thread 继续` 模式 + T1 `KnowledgeSearchGuard`）；blocks-v1 合同进 `agent_contracts.py`（`QaBlock`/`QaAnswer` + `qa_rag_turn_schema`）；`SDKCodexSession.run` 支持按合同选择 output_schema；`GatewayRuntime._execute_assistant_qa` 按租户选路：有已发布客服智能体且配置 kb-service/媒体密钥时走 RAG 路径，否则保持既有零订单回答；`KbServiceClient.for_tenant` 按请求租户重绑定；新增 `AIOPS_GATEWAY_KB_SERVICE_BASE_URL`/`MEDIA_SIGNING_SECRET` 配置接缝（.env.example 已记录，留空即旧行为）。

安全边界：媒体/引用块只能引用本轮检索签发资源与分段（模型伪造即丢弃）；业务问题漏检索时 harness 强制补检索一次，总上限两次；`found` 无检索依据降级 `not_found`；kb 不可用降级 `unavailable` 且文本仍交付；模型不能生成外部 URL。

验证结果：新增 `tests/test_qa_rag.py` 14 项通过；全量 pytest 568 项通过（554 基线）；Ruff、格式、compileall、锁文件、依赖、diff check 通过。acceptance.feature 增 QA-RAG Feature；qa-plan.md 增 QA-RAG-01..05。

已知缺口：mock-first 协议级验证；真实 kb-service canary（栈当前停机，重启为人工决策）、Java BFF blocks 透传与前端渲染留给 #171/#173 及真实媒体验收。

## 2026-09-10：T4 会话与活跃订单上下文（issue #172）

本次分支：`feat/conversation-context`。

已完成：新增 `conversation_store.py`（conversations + conversation_turns 两表，与网关共用 SQLite；scope_fingerprint 隔离、8 轮/8k token 双上限窗口、30 天保留、生成槽 120s 自过期）。`/v1/conversations` CRUD + `active-order` 绑定端点（绑定即归属校验，未授权统一 404）；assistant questions 增 `conversation_id`：Route 1c 活跃订单 follow-up 省略订单号（每轮重校验，撤销即清绑定走 qa），Route 3 会话轮次落库并由 runtime 在 qa 作业完成时回填答案（取消/失败轮次不保留）。qa/diagnosis/FAQ 分流语义零变化；无 conversation_id 的调用完全走旧路径。

验证结果：新增 `tests/test_conversation_api.py` 10 项通过；全量 pytest 578 项通过；Ruff、格式、compileall、锁文件、依赖、diff check 通过。acceptance.feature 增会话 Feature；qa-plan.md 增 CONV-01..04。

已知缺口：前端多设备续聊、BFF 透传、真实停止生成链路属 #173/P0-E2E-REAL；mock-first 协议级验证不替代业务验收。

## 2026-09-09：T1 受限知识检索与媒体资源协议（issue #168）

源分支：`feat/knowledge-search-media`；已通过 PR #176 合并到 `main`，merge commit `c8be858`。

已完成：新增 `knowledge_search` harness guard、RAGFlow 检索结果规范化、不透明图片/视频资源引用、短时租户/智能体版本/会话授权、MIME 白名单、inline 响应和 HTTP Range 支持；Codex 不能选择任意知识库或 `top_k`，单轮最多两次检索。新增 4 项协议测试，覆盖知识库白名单、调用上限、PNG/MP4 资源、跨租户/过期/失效和上游不可用降级。

验证结果：新增 `tests/test_knowledge_retrieval.py` 4 项通过；现有 assistant、QA store、agent contract 和 diagnostic tool 回归 29 项通过；Ruff、格式检查、`git diff --check` 通过；PR #176 的 verify、windows-verify 和 workflow policy 均通过。

已知缺口：当前仅完成 AI-Ops 侧协议和离线 fake 验证，尚未接入生产 `kb-service`/RAGFlow、Java BFF 或浏览器媒体渲染，**未完成业务媒体验收**。下一步为 T2 智能体版本快照和 T3 QA 统一入口接线。

## 2026-09-10：T5 后台智能体管理、编排与草稿调试（issue #171，接口级口径）

本次分支：`feat/agent-debug-run`。

范围决定（业务方，2026-09-10）：`qumall-admin` 页面/菜单/角色接线与 Java 管理 BFF 仓库为范围外；交付口径改为 AI-Ops 管理/调试 API 完整闭环 + 接口工具（apifox/curl）级验收；公网链路沿用同域反代 `api.qumall.qushiyun.com/v1/*`。浏览器验收两条标准记为范围外边界项。

已完成：新增 `agent_debug.py`——`KbServiceKnowledgeClient`（kb-service GET 面最小客户端，按 RAGFlow v0.27.1 源码核实：详情 404/越权经 `accessible()` 租户判定，文档列表 `run` 字段 UNSTART/RUNNING/CANCEL/DONE/FAIL/SCHEDULE）；`KbBindingResolver`（真实发布校验：按发布租户重绑 client，不存在/跨租户→"不存在"，解析中/解析失败→各自原因，kb 不可达→fail closed，接入 `create_gateway_app` 默认构造——此前线上发布被 UnavailableKnowledgeBindingResolver 一律拒绝，本票起配置 kb URL 即真校验）；`run_agent_debug_answer`（草稿配置经生产 `run_customer_qa_answer` harness 隔离预览，不建会话轮次、不读订单，结果带 `debug=true` + `agent_version=agt_…#draft-r<rev>`）。新增 `POST /v1/agents/{id}/debug-run`（EDIT_ROLES，非草稿/非客服 409，跨租户 404，运行失败 502 脱敏）。

安全边界：debug 与生产共用 guard（KB 白名单、两次上限、媒体只引用本轮签发资源）；调试结果永不落库为会话上下文；媒体授权 600s 短时。

验证结果：新增 `tests/test_agent_debug.py` 11 项通过（resolver 四态、debug blocks/媒体、API 角色/隔离/状态机）；全量 pytest 589 项通过（578 基线零回归）；Ruff、格式、compileall 通过。acceptance.feature 增管理调试 Feature；qa-plan.md 增 ADMIN-01..04 与 ADMIN-REAL。

已知缺口：ADMIN-REAL 真实链路 canary BLOCKED（120 KB 栈停机，人工决策点；素材已备）；浏览器页面验收范围外。#173（T6）解锁条件随本票 API 闭环完成而满足（其真实链路同样待 KB 栈恢复）。

## 2026-09-10：T7 智能体运行监控与脱敏审计（issue #174）

本次分支：`feat/agent-metrics`。

已完成：新增 `metrics_store.py`（`agent_run_metrics` 表共用网关 SQLite；行仅含租户/路由/结果/智能体版本/会话/检索状态/检索数/媒体数/延迟/Token/失败码/时间，枚举严格校验，30 天保留写时清理，租户索引）。完成点接线：`_execute_assistant_qa`（RAG 完成/失败、零订单完成/失败）、`_try_customer_rag`（结果打 `agent_version` 标签但对外合同不变）、`_execute_standard_diagnosis`（完成/受阻/inconclusive）、`run_agent_debug`（完成/KB 不可用/运行失败）、FAQ 命中与 CONVERSATION_BUSY（API 层）。新增 `GET /v1/agent-metrics/summary|runs`（`aiops:agents:manage` + VIEW_ROLES；租户取自鉴权 scope，调用者不可指定；跨租户不可达；非法过滤 422）。监控写入全部 best-effort，失败不影响业务请求。

脱敏边界：监控行从构造上不含问题/回答/Prompt 原文、知识库内容、媒体 URL、对象路径或内部 token；真实 QA 原文只在既有 `assistant_questions` 按其保留期管理。

验证结果：新增 `tests/test_agent_metrics.py` 12 项通过（聚合回读、枚举校验、脱敏字段集、30 天清理、API 角色/租户/未认证/422、真实 GatewayRuntime 失败路径集成）；全量 pytest 601 项通过（589 基线零回归）；Ruff、格式、compileall 通过。acceptance.feature 增监控 Feature；qa-plan.md 增 METRICS-01..06 与 METRICS-REAL。

已知缺口：监控页面展示按范围决定（接口级口径）；METRICS-REAL 生产流量验收待 #173 真实 canary 与 KB 栈恢复；真实取消/超时链路（用户停止、模型超时）归 #173/P0-E2E-REAL。

## 2026-09-10：/v1/media 媒体代理路由 + 首启缺陷修复 + 移动云 36 全栈迁移（PR #181）

本次分支：`feat/media-proxy-route`。

已完成：T1 媒体原语接线到 FastAPI——`GET /v1/media/{signed_id}`（URL HMAC 即凭证，浏览器 `<img>/<video>` 无鉴权头；200/206/416，伪造/过期/失效统一 403，媒体面未配置统一 404 无存在性泄露）。`MediaResourceSigner.verify_signed` + `MediaProxy.serve_signed` 新原语；`KbServiceClient.fetch_media` 图片走 `/kb/documents/images/{image_id}`（kb-service 待补透传端点，缺失时图片按预期 404 降级）、视频走既有 download 端点，`tenant-id` 头租户隔离；Range 头透传上游（视频 seek 只取所需字节，不整段缓冲）。`GatewayRuntime.serve_media` liveness 复查：租户当前已发布客服智能体版本与知识库绑定须仍与 grant 一致，停用/重发布/解绑立即失效（先于 600s TTL）。修复 #169 引入的首启缺陷：`create_gateway_app` 误读 `Settings.providers/.model`（应在 `settings.agent`），无 agent_manager 的新部署首启即 AttributeError 崩溃；120 旧实例未踩中仅因其代码先于智能体功能。

前端契约同步：`docs/agents/frontend-api-brief.md` 场景 B 由旧 `{text, reminder}` 更新为 blocks-v1 合同（四类块渲染表、媒体 URL 同域相对路径规则、`unavailable` 降级、`retrieval_status` 枚举、不缓存媒体 URL 坑位），补 `/v1/conversations` 多轮会话合同与 2026-09-10 媒体验收状态节。

部署迁移（经授权）：AI-Ops 网关 + kb-service + RAGFlow 5 容器全栈迁公司移动云 36（30G/16C，与公司 Java 生产栈同机）。网关 `aiops-gateway.service`（8788，aiops 用户，120 同款加固）与 `kb-service.service`（9380）均 enabled；媒体面配置（本机 kb URL + 新签名密钥）就位；数据（gateway.db/租户映射/供应商密钥）迁移校验完整；镜像经 120 逐 128MB 块 md5 校验推送。已知边界：36 与 120/124 内网不互通（同段不同 VPC），诊断数据面在 36 不可达——诊断线暂由 120 网关继续承载，切流与公网入口为人工决策点未动；120 全部服务与回滚快照原样保留。

验证结果：新增 `tests/test_media_api.py` 7 项（路由级：无鉴权头 200、视频 Range 206/416、篡改 403、未配置 404、kb client 路径映射与错误映射含 Range 透传断言、真实 AgentStore liveness 已发布 200→停用 403）+ `tests/test_knowledge_retrieval.py` 1 项；全量 pytest 612 项通过（604 基线零回归）；Ruff、格式、compileall 通过。36 侧实测：网关 `/health` 200、kb `/healthz` 200、kb→RAGFlow 搜索实响、canary 租户零残留库、媒体面坏 ID 得 403（配置生效）。

已知缺口：kb-service 图片透传端点待补（kb-service 仓库改动，视频链路已可用）；RAGFlow 视频解析需租户 VISION 模型；36 公网入口/TLS/BFF 切流等人工决策；真实 canary 素材验收（C1-C7）待跑。
## 2026-09-10：T6/T8 P0 真实 canary 执行手册（#173/#175 可交付部分）

本次分支：`feat/p0-canary-docs`（媒体端点代码与并行会话的 PR #181 重叠，
发现后本分支收缩为纯文档交付——端点、Range 上游透传与 liveness recheck
以 #181 为准；两侧技术评审互检发现已留在 #181 评审意见，含其
`verify_signed` 对非 ASCII URL id 的公网可达 500 缺陷与修法，随独立修复
票跟进）。

范围说明：#173 剩余为真实链路验收（接口级口径，2026-09-10 业务方范围决定）。PR #181 已将 AI-Ops 网关 + kb-service + RAGFlow 全栈迁至移动云 36 并实测活通（媒体面配置生效、kb→RAGFlow 搜索实响），剩余人工决策点为 36 公网入口/TLS/BFF 切流与 kb-service 图片透传端点。

- **`docs/agents/p0-media-canary.md`**：P0 真实 canary 执行手册——环境事实与决策点清单、`aiops-canary` 租户与素材准备、C1-C7 执行序列（管理面发布校验/草稿调试/QA blocks/图片 200/视频 Range 206/416/多轮与忙碌/降级与越权/监控核对）、清理纪律、#173 验收项到证据的映射表。决策点落地当天即可按手册一次跑完。
- qa-plan.md `P0-E2E-REAL` 更新为接口级口径并指向手册。

验证结果：本分支最终仅含文档与验收工件变更；基线 pytest 全绿。

已知缺口：真实 canary 执行待 36 公网切流与 kb-service 图片端点两个决策点；手册使其从"不可执行"变为"决策落地即可执行"。

## 2026-09-11：真实媒体回归阻塞修复（#173/#175）

本次分支：`fix/media-rag-blockers`。

已完成：媒体回源识别 kb-service/RAGFlow 的 HTTP 200 业务错误 JSON（文档不存在映射为 `MediaNotFound`，其他错误映射为依赖不可用）；媒体代理在上游忽略 Range 时本地切片，并按 PNG/JPEG/WebP/MP4/WebM 魔数校正响应 MIME；QA harness 对空 `tool_requests` 使用用户问题执行一次受限检索，无命中/依赖不可用分别交付 `not_found`/`unavailable` 文本，不再直接产生 `QA_FAILED`。

验证结果：新增媒体/QA 回归测试；全量 pytest 通过，Ruff 与格式检查通过。新增 acceptance.feature 与 qa-plan.md 的 P0-FIX-01/P0-FIX-02 追踪场景。

真实验收状态：36 的 `aiops-gateway` 与 `kb-service` 已恢复运行；图片端点可返回真实字节。视频 200 错误包、Range、MIME 和空工具请求需在修复版本部署后重新执行 P0-FIX-02；C7 指标接口仍需要具备 `VIEW_ROLES` 的真实管理身份，不能用 C 端会话代替。
