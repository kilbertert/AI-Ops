# AI-Ops 开发进度

更新时间：2026-08-04

## 当前状态

当前项目处于“只读诊断运行时 + Codex thin harness + Windows 便携交付”阶段。第一版业务动作面仍然关闭，只交付诊断证据、原因判断和工程师下一步建议。

当前已完成的便携化基线提交为 `13806d2`。相关实现分支为 `feat/windows-packaging`；本次文档治理在 `docs/chinese-governance` 分支完成。

## 里程碑

### M0：只读确定性诊断

已完成：

- MySQL、TDengine、Redis 固定参数化查询和安全上限。
- 订单、计费、设备、时序和消息证据聚合。
- 禁止任何订单修改、退款、补发、重算和服务控制动作。
- 合成 fixture、PII/密钥脱敏和基础自动化测试。

### M1：生产只读边界与业务规则

已完成：

- 专用只读身份、SSH 本地转发和 TDengine 严格代理边界。
- OCPP、AYK、YKC、operator 等协议和计费侧差异。
- 30 天生产只读回放与代表性路径抽样。

尚未完成：

- 仍没有工程师确认的真实故障结论，不能宣称业务准确率完成验收。
- 两轮车规则暂不完整，第一版必须低置信度或明确不支持。

### M2：Codex-native thin harness

已完成：

- Codex 负责选择证据工具和形成因果判断，Python harness 只负责身份、只读边界、日志、脱敏、验证和恢复。
- Responses API provider、同一 `base_url` 下可插拔 key slot、真实模型 fixture 验证。
- 运行目录、证据哈希、超时、provider 失败和 thread resume。

边界：真实模型验证和生产只读回放不是工程师确认的真实故障验收，必须在后续记录中分开表述。

### M3：Windows/Linux 便携包

已完成：

- 平台原生私有路径、Windows DACL、外置密钥槽和配置初始化命令。
- SDK 固定版本的原生 Codex 二进制随 PyInstaller 制品打包。
- Windows UTF-8 输出、当前用户 owner、Windows launcher 子进程模型三个真实 runner 缺陷已修复。
- 最终 ZIP 解压目录、最小 PATH、三份 fixture、私有路径、秘密文件和 OpenSSH 检查已固化到构建验收。

验证证据：

| 项目 | 结果 |
| --- | --- |
| 自动化测试 | 155 项通过 |
| Ruff、格式、compileall、锁文件和依赖检查 | 通过 |
| Linux 最终 ZIP 解压验收 | 通过 |
| GitHub Windows runner | CI run `30799312886` 通过 |
| Windows artifact | [artifact 8850200290](https://github.com/kilbertert/AI-Ops/actions/runs/30799312886/artifacts/8850200290) |
| Windows ZIP SHA-256 | `120f7499482a2ce03926c8a30dab4339cb8927b365612b49f4cc2e969adba3d2` |
| 用户 Windows `D:\AI-Ops` | 已在 Windows 本机完成源码外解压和最终 ZIP 验收；合成 fixture 的真实 provider 运行通过，不能替代真实故障业务验收 |

### M4：文档治理

已完成（当前分支 `docs/chinese-governance`）：

- 人面向文档统一中文化。
- 用本文件维护里程碑、证据、缺口和下一步。
- 用仓库级 `AGENTS.md` 强制每个里程碑同步进度和验收文档。
- 根目录 `README.md`、`docs/`、`ops/README.md` 和 `.env.example` 注释已按中文优先规则整理；命令、环境变量、JSON 字段和代码标识保持不变。
- `docs/validation.md` 已区分自动化、生产只读回放、真实 provider、便携包和工程师确认的业务验收，不再把 fixture 或模型调用写成真实故障结论。

### M5：Windows runner 证据交付

已完成（分支 `fix/windows-model-evidence-delivery`，提交 `4730d04`）：

- Windows 源码工作区已通过 Git bundle 恢复完整历史，并建立独立验收分支；私有 key 未进入仓库。
- Windows `uv sync --locked --dev`、CLI help、155 项 pytest、`agent-doctor` 和 `https://api.psydo.top/` provider 探针均通过。
- 首次真实 fixture run 暴露：证据工具采集成功，但模型依赖 Windows sandbox 回读 `evidence/*.json`，结果只能看到元数据并被低置信度阻断。
- 已把已脱敏、有限大小、已哈希关联的 evidence payload 随 tool result 直接交给模型；审计文件仍保留在私有 run workspace，事件日志只记录元数据，不放宽数据库或业务动作权限。
- 修复后的 Windows 真实 run `run-20260803T130133Z-65cd657e-d07d` 完成 `diagnosed/medium`，3 个 evidence、4 个 Codex turn、3 个 tool call；Windows 官方 Codex App 额度未参与。

### M6：Windows 便携包最终本机验收

已完成（2026-08-04，Windows `D:\AI-Ops`，源码提交 `581c396`）：

- 在 Windows 目标机执行 `uv sync --locked --dev` 和 `uv run python packaging/build_portable.py`，生成 `dist\\aiops-diagnostics-0.1.0-windows-x86_64.zip`。
- 将 ZIP 解压到源码目录外的 `D:\AI-Ops-Portable-Acceptance-20260804`，通过 `--help`、`init`、`paths` 和 `agent-doctor --key-slot primary`。
- `agent-doctor` 确认 `base_url=https://api.psydo.top/`、Windows `unelevated` 沙箱和 `business_mutations=disabled`；API key 仍在用户私有 key slot，未进入 ZIP。
- 使用最终冻结包调用一次真实 provider 的 `ykc_amount_mismatch.json` 合成 fixture，run `run-20260803T165338Z-65cd657e-269e` 完成 `diagnosed/medium`，事件文件未发现 `sk-` 密钥内容。
- ZIP SHA-256：`22A16A2B105767493DB82F03862C9B3143D410817EFCBDCCA31FE62830062FEE`。

边界：本次运行使用合成订单，不是工程师确认的真实生产故障；真实业务准确率仍待真实案例验收。

重复验收（2026-08-04）：新解压目录 `D:\AI-Ops-Acceptance-20260804-2` 完成 OCPP、YKC 金额差异和交易数据缺失三次真实 provider fixture run；三个 run 均完成，事件无密钥或业务写入痕迹，验收结束无残留 `aiops.exe` 进程。

### M7：运行中进度、日志与心跳

本次分支：`feat/live-progress-heartbeat`。

已完成：

- `AgentWorkspace.events.jsonl` 作为唯一运行事件源，事件落盘后通过 progress callback 实时推送。
- Codex turn 增加可配置心跳（`AIOPS_AGENT_HEARTBEAT_INTERVAL_SECONDS`，默认 10 秒），并覆盖诊断启动、thread、turn、工具批次开始/完成、合同修复、完成、阻断和中断阶段。
- CLI 默认显示人类可读进度；`--no-progress` 只隐藏显示。`--json` 保持 stdout 为纯 JSON，进度写入 stderr，并返回事件日志和证据日志路径。
- 真实 provider fixture smoke run `run-20260803T194509Z-de1ce5e2-263c` 产生 144 个心跳、5 个 Codex turn、3 个工具批次和 1 次合同修复；stdout JSON 解析通过，事件与运行目录权限/密钥扫描通过。
- 当前 159 项 pytest、Ruff、格式、compileall、锁文件和 diff 检查通过。

边界：进度日志只用于运行状态和追溯，不代表业务结论已经由工程师验收；事件日志不包含证据正文、API key 或数据库密码。

### M8：中央 Gateway MVP 与跨端 run 同步

本次分支：`feat/aiops-gateway`。

实现提交：`efe4605`；已同步当前默认分支基线，最新合并提交：`51113cb`。

已完成：

- 新增固定服务器 Gateway：设备一次性注册、令牌哈希、workspace/tenant 隔离、run 元数据、SQLite/WAL 事件存储、增量事件 API 和 SSE。
- Gateway 服务端复用现有只读诊断引擎；数据库、SSH 和 provider key 只从服务器私有配置加载，客户端不提交 provider URL 或数据库连接参数。
- Windows/Linux 客户端新增 `remote enroll`、`remote doctor`、`remote diagnose`、`remote runs`、`remote show` 和 `remote events`；令牌优先使用 OS keyring，无法使用时回退到私有权限文件。
- 真实 provider Gateway 本机 run `run-20260804T020540Z-9a56710a-06cb` 完成 `diagnosed/high`，34 条事件同步成功；新增 run 元数据脱敏和 profile 路径边界回归测试后，169 项测试通过。
- GitHub Actions CI run `30876603911` 的 Linux `verify`、Windows `windows-verify` 均通过；Windows 最终 ZIP artifact 为 `8879776016`，已完成源码外解压 smoke test。
- 受控跨端验收通过：Tailscale TLS 入口、Linux 客户端真实 provider run、Windows 平台设备身份读取同 workspace 的 run/event、跨租户 `403`、撤销后 `401` 和注册码重用 `400` 均符合预期。
- 最终 ZIP 烟测已增加 `remote --help` 和 mock Gateway 的 `remote enroll/doctor/runs`，并检查 profile/token 私有权限；Windows runner 已重新构建验证通过。

边界：当前是单节点 Gateway MVP。正式多租户上线前必须完成 TLS、OIDC/mTLS、Vault/KMS、PostgreSQL、速率限制、设备撤销和审计保留；本次仍不是工程师确认的真实生产故障业务验收。

### M9：快速上手文档

本次分支：`docs/quickstart-guide`（从 `feat/aiops-gateway` HEAD 切出，因快速上手需引用尚未合并到 `main` 的 `remote`/Gateway 命令与 `docs/gateway.md`）。

已完成：

- 新增 `docs/快速上手.md`，覆盖前置条件、安装、5 分钟离线 fixture 诊断（无需凭据）、生产数据源配置与 `doctor`、实时订单诊断、Codex-native agent 诊断与 `agent-resume`、交互式 `shell`、多端 Gateway 客户端（`remote enroll/doctor/diagnose/runs/show/events/logout` 与服务端 `aiops-gateway serve/issue-enrollment`）、退出码表和常见问题。
- `README.md` 顶部加入快速上手入口并在文档列表置顶，避免上手路径在长 README 中被淹没。
- 文中 21 条命令与三份 fixture 已逐一对照 `cli.py`、`gateway_cli.py`、`gateway_server.py` 与 `examples/fixtures/` 核实；所有内部文档链接均解析成功。

验证：`git diff --check`、`ruff check`、`ruff format --check` 通过。纯文档变更，未新增自动化检查或真实环境验证，未改动任何代码或配置。

边界：快速上手是对既有入口的整理，不改变用户入口、配置、部署方式或安全边界；仍不替代工程师确认的真实故障业务验收。

### M10：统一 diagnose 入口

本次分支：`feat/unified-diagnose-entry`。

已完成：

- `aiops diagnose` 成为统一入口，默认走 Codex-native agent；新增 `--mode {agent,deterministic}`，`--mode deterministic` 走原确定性规则引擎（离线 fixture、CI 回归、provider 不可用时的基准对照）。
- 移除 `agent-diagnose` 命令，其能力由 `diagnose`（默认 agent）承担；`agent-resume`、`agent-doctor`、`doctor`、`shell`、`remote diagnose` 保持不变。
- agent 模式在创建 workspace 前预检 provider key，缺失时给出明确提示并指向 `--mode deterministic`，避免留下空 run 目录。
- 同步更新 `README.md`、`docs/快速上手.md`、`docs/thin-harness.md` 命令示例；`packaging/build_portable.py` 烟测改为 `diagnose --mode deterministic --fixture`，保持离线、不调用模型。

边界：默认 agent 意味着裸 `diagnose "订单..."` 需要 provider key 与模型调用，离线/回归必须显式 `--mode deterministic`；仍不替代工程师确认的真实故障业务验收。

### M11：Windows Gateway 操作文档与错误可见性

本次分支：`fix/gateway-error-details`。

用户在 Windows `D:\aiops` 实机反馈了两类问题：Gateway 注册命令使用了错误的注册码文件名，且 `remote diagnose` 只显示 `interrupted`，没有暴露可追溯的中断原因。

已完成：

- 在 `docs/快速上手.md`、`docs/portable.md` 和 `docs/gateway.md` 增加 Windows `cmd.exe` 完整流程、`cd /d`、`dir`、注册码一次性兑换、`--code-file` 路径排查、`remote doctor/runs/show/events` 的区别和清理步骤。
- Gateway run 持久化并返回经过脱敏和 1000 字符限长的 `error_message`；客户端最终结果与事件均显示 `error_type` 和错误原因，不再只显示 `interrupted`。
- 对已有 SQLite 数据库增加兼容迁移，旧表会自动补充 `error_message` 列。

边界：本次用户反馈使用的订单号尚未形成工程师确认的真实故障案例；错误可见性修复只改善运行追溯，不改变第一版只读和禁止业务动作边界。

## 当前缺口与下一步

1. 由用户在 Windows 本机运行最终 ZIP，重点反馈首次注册、系统凭据库、`remote doctor`、run/event 显示、中文输出和杀毒软件兼容问题。
2. 若用户环境与 runner 不同，重新执行源码外解压、`agent-doctor`、fixture 验证和进度/事件日志检查。
3. 获取并记录工程师确认的真实故障案例，按 YKC 金额、交易数据缺失、状态 2、状态 5、OCPP 计费和 Redis 同步路径分别验收。
4. 正式部署前完成 OIDC Device Flow/mTLS、Vault/KMS、PostgreSQL、速率限制、审计留存、备份恢复和服务级监控。
5. 企业分发前补充 Windows 代码签名、制品保留和升级策略；当前合成 fixture 仍不代表生产准确率。

## 更新规则

每次里程碑提交前必须同时检查：

- `docs/开发进度.md` 是否记录本次目标、提交、验证、缺口和下一步。
- `docs/validation.md` 是否记录新增测试、真实环境结果和失败修复。
- README、部署和架构文档是否与用户入口和安全边界一致。
- 所有“通过”“完成”“验收”结论是否能在 CI、artifact、测试输出或工程师确认记录中找到证据。
