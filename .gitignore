.env
.env.*
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
reports/
.aiops/
node_modules/

# Read-only production source snapshot used to derive diagnostic rules.
backend-v2-domestic/
backend-v2-domestic.zip

# AFK runner (package.json) dependencies
node_modules/

# pnpm lockfiles (repo uses npm)
pnpm-lock.yaml
pnpm-workspace.yaml

# Local working copies of upstream repos used as read-only references
.scratch/
gitlab_token.txt

# AFK runner runtime dirs (not source)
.sandcastle/worktrees/

# AFK runner (package.json) dependencies
node_modules/

# 内部真实环境验收记录（含生产拓扑与账号处置，默认私有，不入库）
docs/qa-plan-real.md
docs/real-acceptance-results.md
