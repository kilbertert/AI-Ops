# AI-Ops 领域词汇表

## 领域词汇表

**诊断运行**：针对一个订单故障问题收集受限证据并生成结构化诊断结果的过程。
_Avoid_: 任意写入操作、运维变更

**订单事件**：一次诊断运行所绑定的订单问题身份与恢复边界。
_Avoid_: 会话、请求（范围不明确）

**证据**：由只读数据源返回、经脱敏并记录来源的事实，用于支撑诊断结论。
_Avoid_: 日志、猜测

**证据日志**：按运行顺序记录证据元数据、阶段和工具结果的可追溯记录。
_Avoid_: 诊断报告（报告是最终结果）

**诊断报告**：依据证据生成、包含摘要、根因判断、置信度和下一步的结构化结果。
_Avoid_: 原始响应

**确定性路径**：不调用模型、按已知规则和离线 fixture 生成结果的诊断模式。
_Avoid_: Agent 模式

**Agent 路径**：由受限的 Codex-native 工具调用收集证据并生成结果的诊断模式。
_Avoid_: 自动写入

**Gateway**：为远程客户端提供注册、运行查询和证据同步的只读诊断服务边界。
_Avoid_: 数据库代理（两者职责不同）

**前端业务接口**：由业务后端或 BFF 面向小程序提供、使用已验证用户身份约束订单与诊断范围的接口。
_Avoid_: 小程序直连 Gateway、用设备令牌代替用户身份

**单问诊断**：用户提交一个独立问题并触发一次诊断运行；第一版不继承其他问题的对话上下文。
_Avoid_: 多轮会话、聊天线程

**可生成报告订单**：数据已经满足充电健康报告实时计算条件的订单，不表示存在待办任务、已持久化报告或未读状态。
_Avoid_: 待办报告、待生成报告

**充电健康报告**：针对一次已授权充电订单，按确定性规则实时计算指标、评分和曲线的只读结果；它可以与诊断报告同页展示，但不构成故障根因判断。
_Avoid_: 诊断报告、电池检测证明、已持久化报告

**健康指标**：充电健康报告中由后端按稳定指标代码计算的单项结果，包含结构化状态、数值、单位、参考条件和不可用原因。
_Avoid_: 前端阈值判断、用展示文案代表计算状态

**指标提问**：用户针对一个健康指标主动发起的单问诊断；指标只提供提问上下文，不能被直接解释为故障根因。
_Avoid_: 指标告警即故障结论、自动诊断

**报告计算作业**：为一个已授权订单异步生成充电健康报告的短生命周期计算过程；作业结果会过期，不表示报告已被持久化或形成待办状态。
_Avoid_: 报告任务、待办报告、报告记录

**身份委托句柄**：业务后端在验证小程序会话后签发的一次性不透明引用，AI-Ops 通过受认证的服务端回查获得当前请求的用户主体和授权范围。
_Avoid_: 裸 user_id、裸 tenant_id、设备令牌、长期自签 JWT

**固定问答**：依据已确认业务文案，按问题标识返回固定问题与固定答案的确定性内容，不创建诊断运行、不调用模型、不读取订单。
_Avoid_: 单问诊断、模型回答、动态订单问答

**平台内容域**：固定问答所属的业务平台范围；第一版为 `consumer`（客户端）和 `operator`（管家端），用于隔离内容目录，不代表认证身份或 Java `clientType`。
_Avoid_: 调用方身份、设备类型、Agent 实例

**固定问答目录**：按平台内容域组织的正式问题、答案和版本集合；由版本化数据制品提供权威内容，前端推荐列表从中选择并随客户端发布。
_Avoid_: 后端推荐排序、待办报告、诊断历史

**前端推荐配置**：由客户端随版本发布的问题展示清单，只包含 `question_id`、展示标题和排序等展示字段，不包含答案。
_Avoid_: 后端实时推荐、答案缓存、权限凭据
